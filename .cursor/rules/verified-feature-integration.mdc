---
description: 功能通过验证和独立审查后自动纳入 Git 与 GitHub 版本管理
alwaysApply: true
---

# 已验证功能自动集成

功能实现、要求的自动化验证和独立审查全部完成，且审查结论为 `ACCEPTED`、Critical／Important／Minor 均为 0 时，自动完成版本管理，不再请求例行确认：

1. 提交该功能的实现、测试和必要留痕，确认功能分支工作区干净。
2. 确认本地 `main` 干净并获取 `origin` 最新状态；远端无分叉时将功能分支合并到 `main`。
3. 在合并结果上重新执行项目完整门禁；全部通过后推送 `origin/main`。
4. 记录功能提交、审查结论、合并提交或快进结果、推送结果和验证证据。
5. 推送成功后只清理本项目自行创建且确认安全的临时工作树和已合并分支。

出现合并冲突、主分支未提交改动、远端分叉、鉴权失败或任一门禁失败时，保留现场并报告具体阻塞，不强推、不降低检查标准。

自动版本管理只包含 Git 提交、合并和推送；标签、Release、部署、生产迁移和生产数据操作仍需对应授权。
