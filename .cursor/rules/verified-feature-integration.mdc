---
description: 按风险验证稳定候选、复用可信证据并自动集成 GitHub
alwaysApply: true
---

# 风险分级、候选验证与自动集成

## 开工分级

每项任务先在现有执行记录写一行：业务范围、Q0／Q1／Q2、判定依据、必须检查、拟省略检查及理由。风险按行为和影响判断；范围不明或执行中扩大时升级。

| 等级 | 判定 | 候选门禁 |
| --- | --- | --- |
| Q0 低风险 | 普通文案、局部样式、非敏感展示；不改变规则、契约、权限、写入或敏感可见性 | 聚焦检查、适用的类型／Lint／格式、轻量独立审查；交互变化补交互测试或冒烟 |
| Q1 中风险 | 普通交互、接口、非敏感校验，且能证明未触及Q2 | 聚焦TDD、稳定候选一次`pnpm verify`、相关集成／E2E、轻量独立审查；真实读写提供真实数据库证据 |
| Q2 高风险 | 权限隔离、个人信息、审计、迁移／约束、事务、乐观锁、并发、资金、安全公共组件或门禁 | 完整`pnpm verify`、风险专项验证、独立Q2；涉及本项目数据库或客户数据路径时运行完整数据库E2E，迁移另做迁移专项 |

前端敏感字段、共享契约／校验器和核心配置按实际影响升级；现有更严格专项门禁继续适用。纯说明文档不伪造TDD，也不启动数据库。

## 最短执行链与证据

环境快速核对→聚焦失败测试→实现与聚焦回归→低成本预检查→固定候选提交→按风险门禁与独立审查并行→修复／复查→文档收口→安全集成。

- 开发循环只跑受影响检查；行为变更和缺陷修复保留真实RED，不删断言、不放宽校验。修改测试辅助导出或`.mjs`时同步检查`.d.mts`和调用方类型。
- 候选记录基线`main`／`origin/main`、提交和文件树、锁文件／迁移／测试配置、运行时和脱敏环境、命令／范围／退出状态、审查对象。记录各阶段耗时、完整verify／E2E次数及每次重跑原因。
- 耗时用于比较返工与重复工作，不承诺固定分钟数，也不设置到时跳过门禁。
- 同一候选、输入和可信环境已有通过证据时不重复全量检查。业务代码、迁移、依赖、构建／测试配置或相关服务变化会形成新候选；先聚焦回归，再重跑失效证据覆盖的门禁。首次失败及处置必须保留。
- 只追加非执行性收口文档时复用业务证据，补做文档、上下文和差异检查。Spec、权限规则、执行指令、测试清单和配置不自动视为纯文档。
- 复用已匹配的Node／pnpm／缓存和可信服务；新worktree缺依赖时用`--frozen-lockfile`和缓存优先初始化。端口可达不证明服务属于当前候选；共享写目录、测试库或服务的任务串行。

## 数据库与独立审查

- 查询、隔离、事务、并发、乐观版本或审计变化即使无schema变化，也运行对应真实数据库测试。仅在验证迁移／约束、数据库环境变化或状态不可信时重建空库；重建前同时核对连接目标、测试标识和独立数据库范围。
- 迁移任务从确认为空的独立库运行全部迁移；容器重建本身不算空库证据。另从上一支持schema加载合成旧数据验证升级，覆盖数据保留、旧数据兼容、预期失败及绕过DTO的直接SQL，按Spec检查NULL、空串、空白和不完整组合。共享历史迁移用前向迁移修复。
- 候选固定后可并行静态审查与测试。Q0／Q1做有范围的轻量独立审查；Q2做独立Q2。审查者核对原始证据并抽查高风险点，不默认重跑全部门禁；覆盖不足、环境不可信或规范要求时再扩验。
- 测试未完成只能给阶段意见。修复形成新候选，独立主体确认问题关闭并核对新证据；没有独立主体时保留成果并阻断自动集成。

## 两批文档与自动版本管理

状态文档只在开工登记范围／风险／门禁，在收口登记实现／证据／审查；中间失败留在任务执行记录。项目状态写摘要，验证与审查引用现有证据；上下文快照按这两批生成。

候选门禁通过、独立审查`ACCEPTED`且无未解决问题时，自动提交、获取远端、确认`main`干净且关系符合预期，再集成并推送。只有主分支未引入新内容、无冲突或额外业务修改、集成树与候选一致（或仅核对过的非执行性收口文档）、依赖／环境／服务证据仍匹配时，合并后省略重复全量业务验证，只做上下文、差异、工作区和关键冒烟。

`main`已前进时，先在隔离分支纳入最新基线形成新候选，执行受影响门禁和增量独立审查。冲突、脏工作区、异常分叉、鉴权／门禁／分支保护失败或证据不匹配时保留现场，不强推、不降级；推送后核实远端包含预期提交。自动范围不含标签、Release、部署、生产迁移或生产数据。

## 场景推演

| 场景 | 必须执行 | 可复用／省略 | 重新审查条件 |
| --- | --- | --- | --- |
| 普通按钮文案／局部样式 | Q0聚焦、适用类型／Lint／格式、轻量审查；交互变化补冒烟 | 不启动数据库；不机械跑完整verify | 触及交互、敏感展示或契约即升级 |
| 普通接口校验 | Q1 RED／回归、一次verify、相关集成；真实写入用真实库 | 候选不变时复用同次证据 | 共享校验器、契约或数据影响扩大 |
| 准入联系人式切片 | Q2 verify、完整数据库E2E、迁移专项、独立Q2 | 同一候选不重复全套 | 个人信息、权限、乐观锁、迁移任一变化 |
| Q2发现SQL约束缺口 | 新候选聚焦SQL负向测试、失效门禁、独立复查 | 未受影响证据说明来源后复用 | 修复影响扩散时扩大复验 |
| 候选通过后`main`前进 | 纳入新基线、核对树差异、受影响门禁和增量审查 | 新候选通过后不因移动到`main`再重复 | 冲突处理或新增业务差异 |
