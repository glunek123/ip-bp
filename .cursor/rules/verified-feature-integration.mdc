---
description: 三级风险、按需上下文、自动验证与安全集成
alwaysApply: true
---

# 三级开发与集成规则

## 选择模式

默认Level 2；命中更高风险即升级，范围缩小时可降级并说明依据。历史记录中的Q0／Q1／Q2分别对应Level 1／2／3，不再用于新任务。

| 模式         | 适用范围                                                                                                                               | 开发与候选门禁                                                                                                                               |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| Level 1 轻量 | 文案、样式、普通页面交互、小字段、简单CRUD、小型Bug；不改变核心规则、权限、敏感可见性、schema或公共契约                                | 相关代码／测试→修改→定向测试→`pnpm check:fast`；交互变化补局部冒烟                                                                           |
| Level 2 标准 | 默认；普通业务功能、接口、业务规则、前后端联动、新模块、非核心Workflow、小型数据模型扩展                                               | 相关Spec／Decision→实现→单元／契约→定向集成→必要局部E2E→`pnpm check:fast`；完整切片或合并前只跑一次`pnpm verify`                             |
| Level 3 严格 | 身份、权限、RBAC、部门／租户隔离、schema／迁移、财务、删除、文件安全、审计、核心事务、幂等、并发、乐观锁、敏感信息、核心架构或质量门禁 | 相关架构／Spec＋短设计→实现→单元／契约／负向与风险专项→完整`pnpm verify`→风险触发的完整E2E／迁移专项→独立Review `ACCEPTED`且0项未关闭finding |

Level 1不默认扫描全仓、更新Spec、写设计／计划、跑完整E2E或独立审查。Level 2只在规则／契约改变时更新对应Spec。Level 3也只加载受影响架构和Spec，不重复确认已批准方案。

## 紧凑执行循环

1. 检查Git现场，读取当前模块的Spec、源码和直接测试；只按触发条件加载架构、环境、Decision或Skill。
2. 用户范围明确即直接实现。仅在产品方向分叉、不可逆影响、关键架构变化、既有规则冲突或关键业务信息缺失时询问。
3. 开发循环只跑受影响测试；行为修复保留能证明问题的RED，配置／纯文档不机械伪造TDD。
4. 同一候选、输入和可信环境已有通过证据时直接复用。修改业务代码、迁移、依赖、测试／构建配置或相关服务后，只使覆盖它们的证据失效。
5. 最终稳定候选按所属Level执行门禁；完整`verify`统一放在完整切片、合并`main`前、核心架构变更或跨模块回归怀疑时，避免开发过程中重复运行。

## 自动化与数据库

- `pnpm check:fast`承担类型和Lint；格式检查、相关Jest／Vitest／Playwright使用现有命令和过滤参数。项目没有可靠依赖图，因此不提供会误报安全的自动`test:affected`。
- `pnpm verify`覆盖context、Spec、类型、Lint、格式、工具／前后端测试和构建，不含数据库E2E。发布前执行完整回归；托管CI未配置时不得声称服务器已验证。
- 身份、认证、会话、授权守卫、权限／数据范围、查询隔离、事务、并发、乐观锁、审计或真实写入路径发生行为变化时，必须运行覆盖该路径的真实数据库E2E；不得只以单元、契约或`pnpm verify`替代。只有验证迁移／约束、环境变化或状态不可信时重建独立测试库。
- 迁移从确认空库运行全部迁移，并从上一支持schema验证合成旧数据升级、失败回滚和绕过DTO的负向输入。执行reset／drop前核对目标、测试标识和授权范围。

## 文档、Skills与Review

- 只有业务规则、字段语义、状态机、Workflow、权限、API契约、数据结构或重要架构决定变化时同步对应Spec／Decision。实现细节、局部重构、文案和样式不扩写设计文档。
- Level 1同会话短任务不更新开发状态；Level 2仅在业务状态变化、跨会话或需要恢复时更新；Level 3保留简短开工／收口检查点。详细证据进入既有验证记录，不建第二套流水。
- 默认单Agent。子Agent只用于用户明确要求、多个独立模块并行、大范围独立审查或Level 3合并／发布前Review；不让多个Agent重复研究或实现同一问题。
- Skill只按实际分支触发；普通CRUD不组合加载设计、计划、TDD、调试、浏览器和验收方法论。

## 候选、集成与清理

Level 1／2门禁通过即可按已有授权提交。Level 3只有在完整门禁通过、独立Review明确给出`ACCEPTED`，且Critical／Important／Minor未关闭finding均为0后才能集成；审查发现问题后的修复形成新候选，必须由独立审查主体确认问题关闭并核对受影响的新证据。合并前获取远端并确认`main`干净、关系符合预期；`main`前进时只重跑受影响门禁。冲突、异常分叉、鉴权或证据不匹配时保留现场，不强推、不降级。

推送后核实远端包含预期提交，再确认任务提交已进入`main`且任务worktree干净、无进程占用；随后`git worktree remove`、安全删除已合并本地任务分支、`git worktree prune`并复核清单。脏worktree先判断是否仍需集成；待清理现场保存为可恢复stash或补丁并记录恢复方式后再删除。自动范围不含标签、Release、部署、生产迁移或生产数据。
