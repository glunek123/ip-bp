---
description: 三级风险、按需上下文、自动验证与安全集成
alwaysApply: true
---

# 三级开发与集成规则

## 选择模式

默认Level 2；按“修改了什么能力”而不是“使用了什么能力”判断风险。普通业务切片复用既有权限、部门范围、审计、事务和乐观锁不自动升级；修改这些共享机制本身才升级。范围缩小时可降级并说明依据。历史记录中的Q0／Q1／Q2分别对应Level 1／2／3，不再用于新任务。

| 模式         | 适用范围                                                                                                                                                             | 开发与候选门禁                                                                                                                                         |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Level 1 轻量 | 文案、样式、普通页面交互、小字段、简单CRUD、小型Bug；不改变核心规则、权限、敏感可见性、schema或公共契约                                                              | 相关代码／测试→修改→定向测试→`pnpm check:fast`；交互变化补局部冒烟                                                                                     |
| Level 2 标准 | 默认；普通业务功能、接口、业务规则、前后端联动、使用既有授权／部门隔离／审计／事务／乐观锁，以及非破坏、失败原子、可安全向前修复的普通增量迁移                       | 相关Spec／Decision→实现→定向单元／契约→必要的切片数据库E2E→`pnpm check:fast`→受影响格式；语义／契约变化运行`spec:check`，普通迁移运行迁移专项          |
| Level 3 严格 | 修改身份认证、RBAC／权限计算、部门／租户隔离、ScopedRepository、共享审计／事务基础设施、跨部门数据边界、财务核心不变量、破坏性迁移／历史数据重写、核心架构或质量门禁 | 相关架构／Spec＋必要的短设计→实现与风险专项→Review／修复／关闭finding→稳定候选→完整`pnpm verify`→风险触发的数据库E2E／迁移专项（主体可用性降级见下节） |

Level 1不默认扫描全仓、更新Spec、写设计／计划、跑完整E2E或独立审查。Level 2只在业务规则、字段语义或公共契约改变时更新对应Spec并运行`spec:check`，不默认运行完整`pnpm verify`、完整数据库E2E或独立Review；多Agent集成候选的一次Final Review和schema／migration等高风险Task审查按[协作路由](../../docs/ai-coding.md#多-agent-协作路由)执行。Level 3也只加载受影响架构和Spec，不重复确认已批准方案；设计落盘、实施计划、范围内测试和本地提交不再逐项请求确认。

## 紧凑执行循环

1. 检查Git现场，读取当前模块的Spec、源码和直接测试；只按触发条件加载架构、环境、Decision或Skill。
2. 用户范围明确即直接实现。仅在产品方向分叉、不可逆影响、关键架构变化、既有规则冲突或关键业务信息缺失时询问。
3. 开发循环可在有本任务未提交改动的工作区运行受影响测试和快速静态检查，不要求先提交、创建evidence或额外worktree；行为修复保留能证明问题的RED，配置／纯文档不机械伪造TDD。
4. 只有正式生成或复用证据时才要求干净固定候选、环境指纹、候选一致性检查和相应互斥。同一候选、输入和可信环境已有通过证据时直接复用；修改业务代码、迁移、依赖、测试／构建配置或相关服务后，只使覆盖它们的证据失效。
5. 开发循环按改动影响选择定向单元／组件、数据库集成和必要浏览器用例，并按需运行`check:fast`；没有可靠影响范围时扩大验证。正式稳定候选直接运行所属Slice门禁。若候选tree未变化，不在Slice门禁前机械重复其已包含的定向单元、`check:fast`、格式、构建或对应E2E；相同候选不因流程周转重复完整门禁。
6. 高风险Task在集成前由独立Reviewer审查；普通Task凭自审、聚焦测试和报告交接，不逐Task增设Reviewer。多Agent的全部Task集成为候选后做一次独立Final Review；Level 3必须完成该Review。finding修复后由Review主体确认关闭，再冻结候选并集中运行完整门禁。Review可为具体finding运行聚焦测试，不默认重跑完整门禁。“集中完成最终验证”不是次数限制；验证失败后的修复形成新候选，必须重新判断并完成其失效门禁。
7. 默认收口顺序为实现与定向测试→必要Review及finding关闭→同步最终业务契约和必要文档→固定候选→所属正式门禁→保存实际证据并补记完成状态→核对收口diff并执行适用文档检查→集成；Review后有实质修改须补充受影响审查。门禁后先集中整理完成状态与验证摘要，解释漂移后再记录上下文快照并执行文档专项检查；若后来仍修正文档，必须重做受影响检查，不沿用旧结果。正式门禁前不预写通过或提前标完成，门禁期间保持候选稳定。Level 2有已配置的对应Slice门禁时使用它；暂无对应scope时，在固定候选上逐项执行本规则Level 2要求的定向检查、必要数据库E2E及迁移专项，并在既有验证记录中列明结果，不伪造scope或自动升级风险等级。Level 3使用完整`verify`，`verify:slice:core-ld`等聚焦scope不能替代它；MVP期间保留现有业务Slice合并门禁，不把全量回归一律后移。最终Git tree与已有可信证据完全相同时复用；多个候选组合成未验证的新tree时，先确认真实集成结果覆盖，再运行所属门禁。没有可靠依赖图时不得声称自动affected验证。

## 自动化与数据库

- `pnpm check:fast`承担类型和Lint；格式检查、相关Jest／Vitest／Playwright使用现有命令和过滤参数。项目没有可靠依赖图，因此不提供会误报安全的自动`test:affected`。
- 新增权限Action时，在开发期先运行现有后端权限目录、前端严格解码器及人员／角色页面的契约测试；这些覆盖不足时才补最小反例，不等最终全量门禁发现目录数量或类型不同步。
- 当前最小架构护栏检查业务模块的项目内相对导入、Prisma Model主责声明，以及Controller的静态数据库导入；无插值动态导入按静态路径处理。计算型动态导入和未来新增的路径别名仍须在相关改动中Review，新增别名时必须同步扩展护栏正反例，不能把未覆盖写法表述为已证明安全。
- 普通`pnpm context:check`报告漂移但不因漂移失败；任务开始结合`git status`／diff判断重叠。`pnpm context:check:strict`在Level 3、Merge、Release及完整`pnpm verify`中阻断未解释漂移。缺失／损坏快照和文档结构错误始终失败。
- `pnpm verify`覆盖严格context、Spec、类型、Lint、格式、工具／前后端测试和构建，不含数据库E2E。发布前执行完整回归；托管CI未配置时不得声称服务器已验证。
- 正式scope与风险级别以`package.json`和`scripts/validation-scopes.mjs`的实际配置为准；当前`customer`、`right-holder`为Level 2，`auth-focused`、`core-ld`为Level 3聚焦scope。开发循环使用已配置的定向单元／过滤E2E和`check:fast`，不把聚焦scope冒充完整Level 3门禁；任何匹配0项的测试范围必须失败。
- 正式Slice按“准备Prisma一次→聚焦单元／契约→prepared静态检查→受影响格式→prepared后端构建→对应E2E”执行；prepared入口只省略已经完成的生成或构建内类型检查，不减少前后端类型、根TypeScript或Lint覆盖。完整`verify`继续只生成一次Prisma Client、只执行一次必要的前端类型检查。
- E2E入口统一解析`backend/.env.test`并拒绝冲突Shell覆盖；环境指纹同时绑定实际PostgreSQL镜像摘要和锁定Playwright浏览器清单，证据只保存本地HMAC指纹及非秘密运行时元数据，不宣称数据库当前数据状态相同。正式Scope固定HEAD和tree，并在每项命令前后核对可观察的候选与环境变化；这与按需使用停止编辑的隔离worktree共同降低干扰，但不冒充对任意本地写进程的只读隔离。
- Evidence v2按tree、固定scope、有序检查集合、环境指纹和协议版本区分验证键。真正开始同键新尝试时先原子发布`running`并使旧成功失效；只有最新`success`可复用，`running`／`failed`／`aborted`、v1、损坏或不完整记录均不可复用。验证键锁与证据短写锁位于Git common directory；统一E2E入口另按共享测试库和固定端口互斥，不串行普通单元、类型或格式检查。
- 身份、认证、会话、授权守卫、权限／数据范围、查询隔离、事务、并发、乐观锁、审计或真实写入路径发生行为变化时，必须运行覆盖该路径的真实数据库E2E；不得只以单元、契约或`pnpm verify`替代。只有验证迁移／约束、环境变化或状态不可信时重建独立测试库。
- 普通非破坏性迁移可保持Level 2，但必须从确认空库运行全部迁移，并从上一支持schema验证合成旧数据升级、失败原子性和绕过DTO的负向输入；失败回滚不代表必须提供down migration。为减少迁移草稿修订后的测试库账本返工，首次部署到独立测试库正式schema前，先在该测试库的随机临时schema验证迁移链、旧数据升级、失败回滚及重试；这只调整既有迁移专项的顺序，不减少验证。删除／重解释字段、历史数据重写、大量数据迁移、跨租户／部门边界变化或不可安全向前修复的迁移升级Level 3。执行reset／drop前核对目标、测试标识和授权范围。

## 文档、Skills与Review

- 只有业务规则、字段语义、状态机、Workflow、权限、API契约、数据结构或重要架构决定变化时同步对应Spec／Decision。实现细节、局部重构、文案和样式不扩写设计文档。
- 模块Spec维护业务契约与AC，`feature-roadmap.md`维护完成态及唯一Current／Next，`VALIDATION.md`维护实际候选、结果和证据边界；`project-status.md`只留恢复所需摘要，开发计划只留实施任务和必要引用。不要为一次完成状态在Spec、计划和状态页重复登记“待门禁／已通过”，也不删除已有必要历史记录。
- Level 1同会话短任务不更新开发状态；Level 2仅在业务状态变化、跨会话或需要恢复时更新；Level 3保留简短开工／收口检查点。详细证据进入既有验证记录，不建第二套流水。
- 影响规则、执行和交付说明的仓库文档必须在最终候选冻结前完成；真正改变业务契约或AC的Spec也须在此时完成相应Review与验证。最终运行结果与Review优先保存在树外证据或PR。正式门禁后只补记真实结果与完成状态，不反向改写已验证业务要求。
- 非执行性收口例外只用于避免**同一已验收业务候选**因补记完成状态而重复本地完整门禁，且须同时满足：①存在可定位的实际通过候选commit／tree、检查结果和证据；②核对从该候选到最终拟收口状态的提交链、累计diff、暂存／未暂存差异和相关未跟踪文件，不只看最后一次提交；上下文快照有漂移时先逐项解释，不直接`context:record`；③全部差异仅为实际完成状态、验证摘要、证据引用及其对应的已核对快照记录，不改变业务范围／契约、权限、状态转换、验收要求，也不混入代码、测试逻辑或输入、依赖、构建／运行配置、生成物、验证逻辑或相关环境变化；④按现有入口完成受影响文档的格式、链接／状态一致性、Spec和上下文检查。纯完成态文字即使位于模块Spec也按语义判断，不能凭`.md`后缀或目录豁免；`docs/context-snapshot.json`也不是整文件白名单。任一条件无法证明即退出例外，按所属风险门禁补审并复验；本条不建立代码、测试或配置变更的通用旧证据复用规则，规则本身的修改不适用本例外。
- 验证记录分别写明实际通过业务门禁的候选commit／tree、此后的完整收口差异、文档专项检查结果及原证据仍适用的理由；不得称收口HEAD实跑过旧候选门禁，也不要求记录写入其自身所在提交的哈希。目标分支变化或合并形成新tree时须核对实际集成结果；远端要求最新提交或合并结果通过检查时照常执行，不以本地例外、路径过滤或伪造成功代替。Release和其他上级强制门禁仍按原要求执行。
- 按可独立验收的任务边界使用子Agent；多个独立模块可并行，大范围独立审查或Level 3合并／发布前Review可交给独立子Agent；不让多个Agent重复研究或实现同一问题。
- Skill只按实际分支触发；普通CRUD不组合加载设计、计划、TDD、调试、浏览器和验收方法论。

## 工作区选择与生命周期

- 同一任务继续、修复或补测时复用原工作区；属于该任务的未提交修改是恢复现场，不为了获得“干净目录”自动暂存、重置或删除。
- 单人串行新任务优先复用当前开发目录或已有可复用开发worktree。复用前确认上一任务已集成、目录干净、无进行中的merge／rebase、无其他写入者或占用进程；从已核实的本地`main`创建新的任务分支。`origin/main`落后时不得覆盖已验证的本地集成结果。
- 只有并行任务、现有目录承载其他未完成现场、需要同时维护不兼容版本，或测试／运行环境确实需要额外目录隔离时才创建临时worktree。已经处于合适隔离环境时不创建第二层，worktree也不是Level固有步骤。
- 一处工作区只允许一个写入者。worktree不隔离数据库、端口、进程或秘密；这些资源继续使用现有测试环境保护和互斥规则。
- 复用目录时核对运行时、锁文件和生成输入；只有依赖链接缺失、运行时／锁文件变化或生成产物失配时重新安装或准备，不因任务变化机械重装。

## 候选、集成与清理

Level 1／2门禁通过即可按已有授权提交。Level 3只有在完整门禁通过、Review明确给出`ACCEPTED`，且Critical／Important／Minor未关闭finding均为0后才能集成；审查发现问题后的修复形成新候选，必须由审查主体确认问题关闭并核对受影响的新证据。自动Slice evidence只绑定干净固定候选的完整Git tree、固定scope、有序检查集合、环境指纹和最新尝试状态；未配置scope的Level 2逐项门禁在既有验证记录中绑定固定commit／tree、实际命令及结果，不伪称Evidence v2。脏工作区、版本或环境不匹配、检查集合不足及非成功状态不得复用。Slice evidence不替代完整Level 3门禁证据。合并前获取远端并确认`main`干净、关系符合预期；最终tree与已验证tree相同则复用。若新tree相对已验证候选仅有上文已核对的非执行性收口（含必要的快照记录），且合并未带入其他变化，保留原候选业务证据并执行文档专项检查，明确记录两个tree的diff；否则新的组合tree执行所属层级门禁。不能仅因“发生合并”无条件扩大范围；冲突、异常分叉、鉴权或证据不匹配时保留现场，不强推、不降级。

### Review 主体的可用性降级（适用于 Level 3）

- **优先**：由**不同模型家族或不同主体**执行独立Review，覆盖高风险点，并给出`ACCEPTED`／`REJECTED`与逐项Critical／Important／Minor计数。
- **降级**：外部独立主体**不可用**时（第三方模型账户余额不足、服务不可达、额度耗尽等），**降级为同模型家族的隔离审查**；审查范围、证据核对与finding计数要求不变，结论必须显式标注`独立性不足`。降级后即可按本条推进集成，不因等待外部主体而阻塞已通过其他门禁的工作。
- **如实记录**：降级时项目状态必须把「自动测试／E2E」「工程门禁」「同模型审查」「独立Review」**分开记录**，独立Review记为`Pending`并注明阻塞原因。**不得表述为"独立Review已通过"**，也不得把该状态写成代码故障。
- **补做**：外部主体恢复可用后应补做独立Review并回填结论；补做结论与降级结论不一致时，以独立Review为准并重新评估已集成内容。

Release与Merge分离。Release候选执行完整`verify`、完整数据库E2E、完整迁移链及适用的环境／备份／恢复检查；生产备份、正式迁移、真实回滚必须明确目标、授权和执行主体，不由普通验证命令自动触发。

推送后先核实远端包含预期提交，并确认任务提交已进入`main`。可复用开发工作区保留目录、依赖和可复用生成环境；确认没有任务现场后安全退出已合并分支，主工作区可回到本地`main`，附加worktree可停在已核实`main`提交的detached状态或直接进入下一任务分支，旧任务分支不再被占用后安全删除。仅为并行或特殊隔离创建的临时worktree，在目录干净且无进程占用后执行`git worktree remove`和`git worktree prune`。宿主工具管理的工作区遵循宿主生命周期，不把手工删除或跨任务改造作为完成条件。脏或归属不明的工作区先判断是否仍需集成；需要清理时先保存为可恢复stash或补丁并记录恢复方式，禁止强删。自动范围不含标签、Release、部署、生产迁移或生产数据。
