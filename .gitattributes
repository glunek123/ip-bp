# Keep repository text files stable across Windows, macOS, Linux, and GitHub.
* text=auto eol=lf

# Unified diffs use a single space to mark unchanged empty context lines.
patches/*.patch text eol=lf whitespace=-blank-at-eol
