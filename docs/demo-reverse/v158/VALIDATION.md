# 阶段 0 本轮验证

版本：见 [INVENTORY.md](INVENTORY.md) 的五文件原始字节 SHA256。日期：2026-09-16。仅验证盘点与若干动态入口，不是全部模块验收，也不是正式系统业务测试。

## 已执行：完整阅读与机械补查

- 磁盘 5 文件；完整阅读 5 文件、19,946 个物理行。连续区间与截断补读见 [COVERAGE.md](COVERAGE.md)。
- TypeScript 5.9.3 对当前 app.js 的 JS 语法解析诊断 0；函数、属性、事件、表单及配置出现点数量见 INVENTORY。
- 全 app.js/HTML 搜索 fetch、XMLHttpRequest、WebSocket、本地存储、HTTP 字面量，并结合全文核对：未发现业务 HTTP 调用；外部字体及商品／示例链接单列。这不是动态全请求证明或外部系统审计。

## 已执行：隔离浏览器

2026-09-16 09:33（Asia/Shanghai），Chromium **153.0.8010.12**，Playwright 为项目已有 **1.63.0**。项目 Node **24.21.0**、pnpm **11.27.0**，每次调用先加载 Use-ProjectRuntime.ps1 并核对。Python Playwright 未安装，改用已有 Node Playwright；未安装任何依赖。

测试直接打开本地 index.html（file 协议），无测试服务器、无假定端口。新建无痕 browser context，视口 1440×1000、时区 Asia/Shanghai；HTTP(S) 请求全部阻断。没有打开或修改用户现有浏览器存档。结束后关闭 context/browser，无后台服务遗留。系统日期未冻结，因此样例的相对日期随运行日变化。

| 场景             | 前置数据                   | 操作                                                                                           | 可观察结果                                                                                                                        | 证据类别                                         |
| ---------------- | -------------------------- | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ |
| V01 初始载入     | 新 context，无本地存档     | 打开入口，等 networkidle，读取 DOM／内存／localStorage                                         | 初始 view-cases；11 个 view；案件 80、客户 8、线索 9、公证 8、证物 18、公证书 7、日历事件 25、账单 2；保存版本 25                 | 实际执行                                         |
| V02 主导航       | V01 状态                   | 依次点击 cases/customers/leads/notary/evidence/calendar/settlement/reports/settings 的侧栏元素 | 每次仅一个对应 view 激活，共 9 个入口均匹配                                                                                       | 实际执行                                         |
| V03 费用兼容导航 | 已加载页面                 | 调用 showView('fees')                                                                          | view-settlement 和 settle-block-fee 激活                                                                                          | 实际执行（函数调用，不冒充用户点击独立费用导航） |
| V04 动态线索表单 | 9 条初始线索               | 点击线索页“新建线索”，不填客户直接点确定                                                       | 标题“新建线索”；表单含客户、权利主体、来源／平台、侵权类型、发现日期、截图／链接等；提示“请填写必填项 client”；弹窗仍开，线索仍 9 | 实际执行；未验证所有字段的全部规则               |
| V05 取消         | V04 校验失败弹窗           | 按 Escape                                                                                      | modal-root 不再 open；未新增线索                                                                                                  | 实际执行                                         |
| V06 案件详情     | 首个初始案件               | 调用 openCase(STATE.cases[0].id)                                                               | view-detail 激活；t-detail 按钮及面板同时激活；当前案显示 4 个折叠区                                                              | 实际执行；其余阶段显隐未全测                     |
| V07 刷新         | 完成上面只读／失败提交操作 | reload 并等待 networkidle                                                                      | 案件仍 80、线索仍 9，存档版本 25                                                                                                  | 实际执行；不能证明所有编辑都保存完整             |

本轮 pageerror 捕获 **0 条**。阻断日志仅出现 Google Fonts CSS（首次与刷新各一次）；不据此推断全部动态路径均不会联网。未下载商品或上传真实案件材料。

源码推导与实测分离：U03 的线索移除、U04 导出缺字段、U11 合单／撤销、U17 空字段／ZIP 分支等仅有静态证据，本轮未实测。供应包“1578 通过”等历史数字未复跑、不计入本报告。

## 文档与交付门槛

已执行并通过：五文件原始字节 SHA256 与盘点起点逐项一致，物理行数一致；文档格式化、pnpm context:record、pnpm context:check（95 个受跟踪文本，包含相对文件链接检查）、pnpm format:check、git diff --check、git diff --cached --check。暂存区为空。Git 报告的 LF/CRLF 提示保留，未对 Demo 执行换行转换。

最终差异仅在既有脏工作区上新增本目录 10 份文档、补记 project-status/deferred-design 并更新 context-snapshot；Demo 的既有未提交修改原样保留，不能把相对 HEAD 的 Demo 差异归为本轮改动。未提交、推送或发布。

未修改可执行工程代码／配置／依赖，因此未运行 pnpm verify、正式系统代码测试、构建或 E2E，也未沿用其旧结果声称本轮通过。文档格式／链接／快照检查不证明语义无遗漏；独立审查尚未执行，18 个观察项保留待提取／待确认。
