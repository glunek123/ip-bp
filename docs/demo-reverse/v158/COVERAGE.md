# 固定版本与完整阅读覆盖

2026-09-16。5 个文件的字节数、SHA256、物理行数固定在 [INVENTORY.md](INVENTORY.md)。读文件采用 UTF-8 原文和物理行编号；行数包括空行，不使用忽略空行的 Measure-Object -Line。未修改 Demo，未将生成索引写回 Demo。

## 阅读覆盖台账

下表合并展示连续完成区间，不冒充单次工具调用大小。实际按数百行分段阅读；输出截断处另行补读。搜索、AST 和浏览器检查均是完整阅读之后的补查，不替代全文。

| 文件       | 完整覆盖区间（含首尾） | 状态／阅读内容                                                                 |
| ---------- | ---------------------- | ------------------------------------------------------------------------------ |
| app.js     | 1–1100                 | 完成：初始常量、案件／客户种子、通用弹窗、字段工具、演示扩展                   |
| app.js     | 1101–2650              | 完成：持久化、律师配置、16 阶段、筛选、上传、退费／判决／二审／付款            |
| app.js     | 2651–4250              | 完成：阶段提交、日历同步、批量文书／ZIP、过滤、归档、自动流转、样例补齐        |
| app.js     | 4251–6350              | 完成：费用、详情、客户档案、结算计算与发起、资产、导航及合并配置               |
| app.js     | 6351–7800              | 完成：合并、详情编辑、客户子表单、批量操作、导出及全局事件                     |
| app.js     | 7801–9350              | 完成：线索全部入口、跨模块转入、CSV、看板／报表、公证配置与部分种子            |
| app.js     | 9351–11000             | 完成：证物／日历／结算种子、公证全流程、开箱／调查／物流、出证／退货、证物渲染 |
| app.js     | 11001–12550            | 完成：证物编辑、日历提示、结算账单、数据迁移、设置模版、文件管理               |
| app.js     | 12551–13215            | 完成：文件下载、各详情折叠区、一二审／执行／结案、日志及初始化                 |
| index.html | 1–450                  | 完成：文档入口、外部资源、侧栏、头部、案件列表开头                             |
| index.html | 451–1752               | 完成：案件列表全部静态快照及视图闭合                                           |
| index.html | 1753–2250              | 完成：案件详情、客户列表、客户详情／主体／联系人                               |
| index.html | 2251–2600              | 完成：资产、客户结算、报表、线索表头                                           |
| index.html | 2601–2900              | 完成：线索快照、公证阶段列表、证物页开头                                       |
| index.html | 2901–3250              | 完成：证物筛选／列表、日历、结算页开头                                         |
| index.html | 3251–3600              | 完成：客户结算／账单、律师结算部分                                             |
| index.html | 3601–3900              | 完成：律师结算／账单、费用中心、设置迁移                                       |
| index.html | 3901–4043              | 完成：设置／模版、外置浮层、弹窗根、toast、脚本引用及文档结尾                  |
| styles.css | 1–350                  | 完成：tokens、reset、壳层、侧栏、头部、按钮开头                                |
| styles.css | 351–850                | 完成：按钮、状态、统计、筛选、表格及案件 Tab                                   |
| styles.css | 851–1350               | 完成：卡片、字段、时间轴、附件、详情、进度、布局、弹窗开头                     |
| styles.css | 1351–1780              | 完成：弹窗、表单、多选、toast、批量条、图表、日历／提示、结算 Tab              |
| styles.css | 1781–2110              | 完成：详情、阶段色、线索链接／协议／结算编辑、折叠、文件清单                   |
| styles.css | 2111–2323              | 完成：文件、付款、合并、证物时间筛选、统一文书上传                             |
| DESIGN.md  | 1–335                  | 完成：v1.5 全文；视觉约束及示例提示是说明，不视为实现                          |
| README.md  | 1–30                   | 完成：入口、五文件来源、三件套指纹、供应历史验证和审核边界                     |

补读记录：app.js 首次长输出的 389–800、781–855；6471–6544；9491–9507。index.html 154–221。补读覆盖了当时输出截断的区间；当前最终覆盖无行号空洞。

## 补查方法与局限

- 使用项目已安装 TypeScript 5.9.3 解析 app.js（ScriptKind.JS）；语法诊断 0。遍历函数声明、显式 window 函数、顶层变量、对象属性、属性赋值、复合修改候选、弹窗与监听器调用。
- 用逐行匹配登记 HTML 和 JS 模板内 on* 属性以及 data-* 绑定；同文件同表达式合并位置，保留动态插值原文。该扫描不是 HTML 完整解析器，也不展开所有运行时字符串。
- 276 个 k/key + label 配置对象含字段／列配置；未声明 required 不等于无提交校验。属性赋值索引保留 DOM 和临时字段，防止在盘点阶段错误剔除。
- 语法容器归属不是别名分析。所有残余动态键／临时属性实体归属问题保留 U18，不包装成已完成业务模型。
- 全文读取及机械复核由主代理完成。本轮未开展最终“全新上下文子代理”交叉审查；该审查在模块提取和聚合之后进行。

## 版本恢复约束

继续提取前重新计算五文件原始字节 SHA256。任一不匹配时先登记输入变更并核对影响；不得沿用本版行号，亦不得直接覆盖本版盘点。供应包里的历史备份与测试脚本已不在本次 5 文件范围，不能用于声称本轮完整性或实测通过。
