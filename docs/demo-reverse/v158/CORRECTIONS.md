# Demo 缺陷与文档修正口径

2026-09-16。用户通过第二次模块检查，授权能够确定的问题直接在文档中修正，不确定的业务取舍提问。本文件是**提取结果的纠偏输入，不是 Demo 补丁或已经实施的正式设计**。固定源码事实仍见各模块及 [观察验证](MODULE-VALIDATION.md)，不得改写 AS-IS 来掩盖 Demo 行为。

## 可以确定的修正方向

下表均指 Demo 中的实现问题或演示简化，不是现有正式系统的缺陷。修正验收为未来场景，**尚未实施、尚未执行修正后验证**。

| 编号                      | Demo 事实与证据                                                                                    | 文档采用的修正口径                                                                                                | 修正验收／关联                                                         |
| ------------------------- | -------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| FIX-01 导入残留           | 取消 CSV 弹窗后再开、不选文件仍能导入旧数据；M01 LT06                                              | 每次打开、取消及文件读取失败时清空本次导入草稿；提交只允许当前已展示并完成解析的批次                              | 选 A→取消→重开→不选文件，不能导入 A；L-D07、DD-02/04                   |
| FIX-02 数字符号丢失       | `numOf('-2')=2`；M01 LT02/08                                                                       | 不把非法字符删除后当成合法数字，不静默改变正负号；解析失败展示字段错误。允许负数的具体业务范围另行确认            | 输入 -2 保留符号后按字段规则接受或拒绝，不能保存 +2；L-D06、DD-05      |
| FIX-03 编号混写           | 一审案号编辑写 `caseNo`，与业务号同键，而展示另读 `no`；M02 CT02、字段字典                         | 业务单号、诉调号、一审法院案号、执行案号分开定义和读写；调整法院案号不得覆盖业务身份。具体编码规则仍待 C-D01      | 更改法院案号后业务号和案件关联不变，列表详情显示同一法院案号；DD-01/04 |
| FIX-04 合并选择失效       | 用户选第二案案由，结果仍取第一案；M02 CT07，A:L6450–6484                                           | 表单允许选择的冲突项必须按最终选择写入；若字段必须固定，改成只读并说明，不展示无效选择                            | 两案案由不同，选 B 后新案案由=B；C-D07、DD-01/02                       |
| FIX-05 清空开庭日不清日程 | 同步函数空值早退，自动日程和推进日期遗留；M02 CT05，A:L2744–2782                                   | 清空或更正开庭日期时同步移除／更新由该字段生成的日程与派生触发日期；不删除手工日程。BD-10已确认保留按日期自动推进 | 日期 D→空后不存在 D 的自动开庭事件及旧推进日期；DD-02/05               |
| FIX-06 记录写读分离       | `advanceCaseStage` 写 timeline，详情读 log；M02 CT06                                               | 用户可见的流程历史必须读到同一次真实动作；记录格式统一或显式转换，不把缺失日志补成虚构办理                        | 执行推进后详情立即显示一次前后状态与实际时间；C-D10、DD-02/06          |
| FIX-07 新建混入演示数据   | 新案生成被告、费用、链接；M02 CT01。新客户也调用 customerExtras；M10 源码 A:L1060–1083、L7011–7025 | 演示种子与真实录入路径分离。新建对象仅含用户输入、明确默认值和可说明的派生数据，不生成虚构当事人、金额或材料      | 新建空业务记录无模拟明细；DD-01/05/06                                  |
| FIX-08 归档冒充结算       | 案件详情按阶段展示“已结案结算”；M02 字段／C-D08                                                    | 归档、结案、收款、结算不得互相冒充完成证据；展示分别取各自事实。各自完成条件仍待财务／业务确认                    | 只归档而无结算事实，不显示结算已完成；DD-02/05/08                      |
| FIX-09 删除提示不符       | 提示包含线索，但仅删除案件且关联保留；M02 CT08                                                     | 提示准确说明实际影响；正式留存策略未决定前不推导级联删除，不以修正提示代替关联一致性设计                          | 操作前说明与操作后实体变化一致；C-D09、DD-01/06                        |
| FIX-10 识别失败伪造字段   | 权属识别无匹配时用文件哈希生成号码、名称、日期并可保存；M10 A:L6029–6075、L7229–7238               | 无法识别的字段保留未知；自动识别输出是待核对候选，不用示例替代真实值，更不能覆盖已有正确字段                      | 不可读文件不生成看似真实的证书号／有效期；DD-01/06/07                  |

代码位置 A 指 [demo/app.js](../../../demo/app.js)，固定版本见 [INVENTORY](INVENTORY.md)。M01/M02 完整证据分别见 [线索事实](modules/M01-leads.md)、[案件事实](modules/M02-cases.md)及[字段字典](modules/M02-cases-fields.md)。这里确定的是消除错误的方向，不擅自决定未知的业务枚举、授权范围、财务口径或留存期限。

## 后续模块的确定纠偏

下表与FIX-01～10同样只修正文档口径；场景是未来修正验收，未执行修正实现。依据模块均链接固定版本源码，动态证据在 [CONTINUATION-VALIDATION](CONTINUATION-VALIDATION.md)。

| 编号                                    | Demo问题及依据                                                  | 修正口径／未来验收                                                                                                                                      |
| --------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FIX-11 不稳定ID／名称关联               | M10 CU02/03、M04 FT04；新客户按数量、费用按毫秒生成；改名掉历史 | 身份不能依数组数量／显示名；删除再新增不与存量碰撞，改名后原业务仍正确关联；具体编号政策待定                                                            |
| FIX-12 调查费重复计费                   | M03 NT02，A:L9961–9973；同金额同时写feeP/investFee              | 只作为调查费记录，不能多记一笔样品支出；输入100后调查100、样品不受此操作增加                                                                            |
| FIX-13 物流匹配分叉                     | M03 NT03/06，A:L9989–10188；批量只看expressNo、混包只查首号     | 单条／批量读同一物流关联，每个包裹核对；不匹配或无法确认保留人工待处理，不按空影子字段队列猜案                                                          |
| FIX-14 公证绕过／转案丢字段             | M03 NT04/05，A:L10661–10678、L10905–10917                       | 所有入口遵守同一动作规则且保存；转案带来源身份、客户主体及实际已有字段，不虚构法院案号；BD-01/02/12限定方向，必填细节待定                               |
| FIX-15 已处理费用改删／状态丢失         | M04 FT01/03/05、FS09，A:L4268–4315、L4724–4741、L5065–5213      | BD-06：已发起／已支付禁止直接改删，所有入口走更正／冲销；退款去向与办理状态分开，不转换为未发起；明细／中心保持同一事实                                 |
| FIX-16 结算错客户、零值与全局公式       | M06 ST01/02，A:L5488–5515、L5692–5729                           | 按明确客户关系计算，不取首客户兜底；显式0≠缺失；BD-08案件修改只影响该案，改A公式后B及客户协议不变                                                       |
| FIX-17 批量费用清零／转案漏费／净额发起 | M03 NS09、M04 FT02/FS06                                         | BD-04保留每条原费、改费明确；BD-05原支出与退款分别提交；BD-12转案完整关联各项原费，不重复生成。验收两条不同费用归档后不变、转案前后同笔身份与金额可追溯 |
| FIX-18 证物条件报错与关联不一致         | M05 ET01/02，A:L3722–3737、L11220–11350、L12000–12045           | 消除未定义shop回退；名称缺失可明确报错／留未知，不能崩溃半提交；阶段／查看／编辑采用明确同一关联集合；多本公证书不能默默只更新首本                      |
| FIX-19 合单丢收付方／撤销错位           | M06 ST03、SS07，A:L11876–11889、L11958–11963                    | BD-07允许跨方，但每条保存并展示收付方和稳定源行身份；重排后撤销仍恢复对应项，不能用第一方或数组位置代替                                                 |
| FIX-20 发起即称完成／多源统计           | M06 ST04、M10 CU05；A:L5736–5850、L5277–5289、L5934–5972        | BD-09标已发起，完成取财务结果；相同统计概念采用明确来源，不让旧副本／月份去重遮蔽新明细。实收0时不能显示已完成结算                                      |
| FIX-21 导出遗漏                         | M09 DT01，A:L1107–1121对比L12136–12149                          | 明确导出范围并覆盖需迁移的账单／模板；导出→隔离恢复后逐集合核对，不能只提示成功                                                                         |
| FIX-22 导入失败污染内存                 | M09 DT02，A:L12160–12198                                        | 先验证完整候选包与关联再替换；失败保留原状态，不能先STATE赋值后报错；缺customers样例不得清空旧案件                                                      |
| FIX-23 空数据当损坏／保存失败无反馈     | M00 XS01/02、M05 ET03、XT01；A:L1107–1175、L9678–9695           | 区分空集合、缺失及损坏，真实业务不自动回种；失败明确提示，不用内存成功代替持久成功；不以此授权修改Demo演示补齐策略                                      |
| FIX-24 识别／模板伪作原文               | M09 DT03、M00文件机制；A:L12263–12273、L1930–2036               | 不可读模板不能冒充用户文件内容；材料名不等于原件，无法识别保留未知，确认后才采用候选；真实格式范围待定                                                  |
| FIX-25 时间混用                         | M00 XT02，A:L397–401、L12326–12331                              | 业务日期与时间戳使用明确一致时区；本地00:30不拼出前一天日期＋当天时分。BD-10保留自动推进，但延期／清空后同步派生触发                                    |
| FIX-26 报表金额与标签                   | M08 RT01/RS01，A:L7589、L9250–9259                              | 数字运算不拼接；BD-11保留amount平均并更名“已归档案件平均标的额”；100＋200导出应数值300，不取closeAmt改算法                                              |
| FIX-27 旧换机说明                       | M09 DS05，A:L12200–12209                                        | 说明引用当前三件套和必要文档，不再称旧单HTML自包含；本轮只写纠偏，不改Demo                                                                              |

本表没有擅自把“合并原案转公证归档”“跨收付方合单”“按日期自动推进”“用标的额算归档均额”列为错误；这些分别受BD-03/07/10/11确认。原费用的零值、舍入、冲销权限和保留期限等细节仍待审核。

## 已提问并确认

| 问题                      | 当前 Demo                         | 建议供用户选择                                     | 状态                                        |
| ------------------------- | --------------------------------- | -------------------------------------------------- | ------------------------------------------- |
| Q-01 线索转公证后如何保留 | 转出后删除线索且复制不全；L-D05   | 保留来源线索只读并关联公证                         | 用户已选此项；BD-01                         |
| Q-02 跨阶段录入是否补录   | 新建／编辑可选择后期阶段；C-D02   | 普通录入逐步推进，历史补录单独入口                 | 用户已选此项；BD-02                         |
| Q-03 合并源案归属         | 删除源案，额外生成公证归档；C-D07 | 原建议不生成公证记录，用户选择沿用 Demo 转公证归档 | 以用户决定为准；BD-03；不判转公证归档为缺陷 |

准确决定及尚未确认的细节见 [DECISIONS](DECISIONS.md)，不以建议覆盖用户选择。

其他待决口径继续保留在各模块候选文档，完成相关模块后合并提问，避免将同一跨模块问题重复询问。这些问题不阻塞继续提取源码事实；它们阻塞对应正式规则的定稿。
