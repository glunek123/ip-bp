# M00 共享机制：候选输入

依据 [M00事实](M00-shared.md)。这些是后续设计输入，不是本轮新增基础设施／接口的授权。

| 编号  | 模型／规则方向                                       | 未决与映射                                                                                                  |
| ----- | ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| X-C01 | 持久化成功、内存更新、演示播种分开                   | 正式数据不得因空表、版本更新或缺字段重播虚构业务；失败须明确，迁移先验证且保留原文；DD-01/02/06，XS01/02/05 |
| X-C02 | 业务日期、时间戳、时区和金额解析统一定义             | 日期含义、时区、金额精度／币种／负数允许范围UNKNOWN；不无声改符号／把非法值变0；DD-05，XS03及FIX-02         |
| X-C03 | 材料记录与文件实体／内容分离，识别结果作为候选       | 点击／拖拽一致，异步回填绑定本次目标，不覆盖别案；真实存储、授权、大小和格式规则待定；DD-04/06/07，XS04     |
| X-C04 | 操作前置校验与操作者身份／范围在正式服务端设计       | role文字、隐藏按钮和前端筛选不是授权；INPUT-001单独作为已确认方向；DD-03/09/10                              |
| X-C05 | 身份、稳定关联与审计不依赖显示名称、毫秒ID或演示日志 | 不同模块的重复记录／投影要有明确来源；并发、重试和事务方案尚未设计；DD-01/02/06/08                          |

正式设计、API、业务代码、数据库迁移和正式业务验收均未产生。未知项不以通用工程经验补成已批准业务需求。
