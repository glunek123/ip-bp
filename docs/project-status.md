# 当前开发状态

更新日期：2026-09-15。本文件是恢复上下文的第一入口；历史报告只对当时版本有效。

## 当前阶段

**通用工程底座本轮完善已完成；Demo 部门审核中，尚未进入正式业务开发。** 用户已批准 FND-002 通用能力完善、本地 Git 和后续设计清单；没有业务实体、表、迁移、权限或工作流实现。

## 当前任务

任务：FND-002，通用底座完善与后续设计清单。状态：已完成。

- 已建立本地 main 和初始基线提交 046f8be；无远程或托管 CI。
- 已实现 JSON 写请求与 204、健康 API 分离、安全日志路由和代码位置；独立代码审查无阻断项，建议的边界测试已补充。
- 已检查组件入口，明确选择器/表格/全量入口的第三方类型缺陷；未放宽类型或改变依赖。
- 已创建 [后续设计清单](deferred-design.md)并接入 AGENTS、AI 规范和架构入口。
- 最终 verify 和跨端测试通过，本次代码、文档及快照共同保存为本地交付提交。提交记录使用 git log 核对，避免文档嵌入自身提交号。中断恢复先执行 context:check 并检查 git diff，不得盲目刷新快照。

## 已实现

| 能力       | 当前事实                                                                              | 边界                             |
| ---------- | ------------------------------------------------------------------------------------- | -------------------------------- |
| 工程       | Vue/Nest/pnpm 模块化单体与本地 PostgreSQL                                             | 无业务表和迁移                   |
| 接口与日志 | 健康/OpenAPI、输入校验、统一错误、请求编号；注册路由及安全项目文件位置                | 无业务审计或生产监控             |
| 浏览器 API | requestJson 支持 GET/POST/PUT/PATCH/DELETE、JSON、204、超时取消；getHealth 运行时校验 | 不含上传、身份会话或业务字段错误 |
| 页面       | 工程健康、加载、错误、重试和取消                                                      | 无业务工作台                     |
| 版本与检查 | 本地 Git、文档快照、类型/Lint/格式/单元/契约/构建/E2E                                 | 无远程强制检查                   |

## 未决与限制

具体事项仅在 [后续设计清单](deferred-design.md)维护，此处按输入分组，不复制完整待办。

- **等待 Demo 字段/规则确认**：DD-01 至 DD-08 的实体、流程、权限范围、页面/API、金额/报表、附件、AI/业财业务映射。
- **另待外部条件**：登录方式、存储、AI 供应商、业财协议，以及 OPS-01 生产环境和 CI-01 托管平台。Demo 文档到齐不等于这些条件齐备。
- **独立兼容项 CMP-01**：按钮、表单、输入、日期选择器的声明检查通过；选择器、表格及全量入口失败。首个相关页面前必须完成兼容方案与验证。结果见 [本轮验收](foundation-improvements.md)。
- 工程仍仅适用本地；ESLint 9 和部分间接依赖的上游弃用提示保留，版本升级单独评估。

## 最近验证

FND-002 于 2026-09-15 使用项目 Node 24.21.0 / pnpm 11.27.0 完成冻结锁文件离线安装及 pnpm verify：文档一致性、类型、Lint、格式、工具 12 + 前端 24 + 后端 23 项测试（共 59 项）、前后端构建全部通过。独立 PostgreSQL 测试库上的 pnpm test:e2e 两项 Chromium 用例通过。独立只读审查无阻断项；补充响应读取取消/超时及日志脱敏用例后已纳入最终检查。

验证后仅更新文档结果，再执行格式与快照一致性检查、Git 差异核对。完整证据及组件失败边界见 [本轮验收](foundation-improvements.md)。组件探针失败不被正式工程检查覆盖或豁免，明确保留 CMP-01。

## 下一步

等待用户提供字段、规则文档；收到后登记版本、来源、确认依据，逐项检查后续清单，再建立需求—设计—实现—测试映射。未齐备项保留等待状态，不能直接批量生成业务代码。CMP-01 独立兼容修复必须在首个受影响页面前完成。
