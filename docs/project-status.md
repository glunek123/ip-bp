# 当前开发状态

更新日期：2026-09-20。本文件只保留恢复当前工作的最小事实；此前的实现与验证流水见[历史状态](project-status-history-through-2026-09-18.md)，详细验证仍以各候选和[验证记录](spec/v0.1/VALIDATION.md)为准。

## 当前阶段

首批正式业务开发：认证与客户最小主数据链路已形成内部闭环；Next Slice前置的Team主数据与管理授权边界已通过Level 3门禁与独立复审。业务路线和完成判定以[功能开发路线图](feature-roadmap.md)为准；生产交付能力与业务Slice分开管理。

## 当前任务

**TEAM-AUTHZ-ARCH-001（实现与Level 3验证完成，待决定集成方式）**：隔离分支`codex/team-permission-architecture`建立最小Team主数据、现有裸`teamId`的安全兼容迁移、六项管理Action和服务端Grant Boundary。范围不含管理页面／HTTP接口、角色模板Grant编辑、客户准入状态机或未来业务表；未执行生产迁移、发布或真实数据写入。

当前实施顺序：

1. Team、迁移、管理授权、测试和事实文档均已完成。
2. 下一步由维护者决定是否集成本分支；业务侧可开始人员账号、部门成员与角色分配Slice。

## 已实现

- 系统统一名称为“品维·知产业务管理系统”，同一系统服务品维部与知产部，服务端承担部门数据隔离与授权。
- 本地账号登录、会话、CSRF、限速，以及客户草稿建档／维护、一位准入联系人和权利主体最小关联已形成明确范围内的内部闭环。
- Team已成为部门直属正式主数据；成员、角色分配和客户以组合外键保证Team与Department一致，停用保留历史并阻止新增／恢复绑定。
- 服务端新增人员、Team、角色读取／管理／分配六项Action；角色分配逐项验证操作者对目标成员和待授Grant的实际覆盖，不使用首位管理员、角色名称、固定ID或前端菜单绕过。
- 工程已有Node/pnpm锁定运行时、上下文与Spec检查、类型／Lint／格式／单元／契约／构建门禁及独立测试库Playwright。
- 普通context检查现报告漂移但不阻断，strict模式继续阻断；标准记录已与状态文件机械更新解耦。客户／权利主体／认证已有显式Unit和数据库E2E入口，0测试匹配会失败。
- Slice runner拥有固定的命令与规范化检查ID，在首项检查前锁定干净tree、末项检查后确认tree未改变，全部成功后才自动记录evidence，调用者不能自报检查集合；证据位于Git common directory，只记录干净固定候选，并以Git tree、环境和精确检查集合在同仓库worktree间复用。同一tree可保留多个范围，commit变化但tree相同仍可复用，新的组合tree仍需完整门禁。
- 完整`verify`已改为只生成一次Prisma Client、只运行一次`vue-tsc`，保留strict context、Spec、类型、Lint、格式、工具／前后端测试和双端构建。

## 未决与限制

- 当前下一业务Slice“人员账号、部门成员与角色分配闭环”仍属于Level 3；本任务只解除其Team完整性和授权上限前置阻塞，不实现该Slice的UI/API。
- E01后续人员管理／MFA／找回密码／OIDC、E02私有对象存储、生产环境与CI仍按[后续设计清单](deferred-design.md)和路线图分别推进。
- 项目没有可靠依赖图；本轮不实现自动affected验证或自动文件归属。新的组合tree仍须在最终tree执行一次完整工程门禁。
- 生产备份、正式迁移、部署、真实回滚和生产数据操作不在本任务授权范围。

## 最近验证

- 2026-09-20基线完整`pnpm verify`通过：工具35项、后端148项、前端125项及双端构建。
- 最终`pnpm verify`退出0：上下文与Spec（53 REQ／55 AC／11 BQ／36 SD）、工具35项、后端168项、前端125项、类型／Lint／格式和双端构建全部通过。
- 隔离测试库从空库顺序应用13份迁移；最终完整PostgreSQL／Playwright 48／48通过。覆盖不存在／跨部门Team外键拒绝、Team部门不可变、停用历史保留且新绑定拒绝、三表UUID映射与NULL保留、共享角色不补权、迁移失败回滚、Grant Boundary正负路径，以及停用／撤权并发锁和事务残留。
- 独立Level 3复审经过问题修复和多轮复核，最终`ACCEPTED`：Critical 0、Important 0、Minor 0，未发现生产实现、迁移、授权或测试假绿问题。

## 下一步

可以开始“人员账号、部门成员与角色分配闭环”。本任务不自动获得分支集成、生产迁移、发布或真实数据操作授权。
