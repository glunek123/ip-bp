# 当前开发状态

更新日期：2026-09-17。本文件是恢复上下文的第一入口；历史报告只对当时版本有效。

## 当前阶段

**首批正式业务开发：运营端优先，共享业务能力，当前实施最小权限／部门隔离与客户基础闭环。** 律师端、公证处端和客户端后续复用同一业务能力并分别限制动作、数据范围、可见字段和页面体验；不建设四套后端，也不以隐藏按钮代替服务端授权。

## 当前任务

CUST-RH-Q2-FIX-001待复审：候选`70c4b41`独立Q2为REJECTED（Critical 0／Important 2／Minor 1）。现已保持070000迁移不变，以第9份前向迁移修复数据库纯ECMAScript空白名称绕过；详情页在客户／主体参数切换时清空旧展示、取消旧请求并忽略旧响应；投影E2E先读后写，消除候选读取与关联竞态。修复后详情页8/8、主体专项11/11、完整verify（工具19、后端98、前端100）及数据库E2E 38/38通过，空库9份迁移和逐步升级／失败回滚通过；当前待独立Q2复审／未上线，仍不触及开发库、生产、真实数据或范围外能力。

CUST-RH-001C候选证据已齐：Task 1/2前后端固定至`c4891d1`，主体PostgreSQL／浏览器专项10/10通过；独立测试库从实测空库应用8份迁移，上一支持schema的旧客户保留、组合部门约束和失败原子回滚均通过。固定执行树`3a93717`的完整verify及数据库E2E 37/37通过；状态保持待独立Q2审查／未上线。临时协作记录迁入既有排除目录`.local/sdd`，不进入候选或上下文快照；不修改检查器排除规则。证据唯一记录于[验证记录](spec/v0.1/VALIDATION.md)。

CUST-RH-001进行中：用户于2026-09-17确认TD-SLICE-CU-RH-01的推荐字段门槛并要求实现，名称必填，信用代码／地址／法定代表人／职务可空。本轮授权该最小切片的前后端、开发／测试库向前迁移和自动化测试；明确排除主体编辑、删除、解除关联、默认主体、共享主体编辑、多联系人、权利资产、协议、合作状态、生产迁移、发布和真实数据。任务按Q2执行，原因是新增共享业务记录、多对多数据库约束、客户数据范围、个人／企业信息、乐观版本、事务和审计；必须完成真实RED／GREEN、聚焦回归、完整`pnpm verify`、空库与上一支持schema迁移专项、完整数据库型Playwright及独立Q2。E01只阻断真实身份联调，E02不阻断本无文件切片；本轮拟省略生产迁移、真实SSO／对象存储联调和范围外人工验收。

CUST-RH-DESIGN-001已完成设计稿：用户于2026-09-17引用会话“开发准入联系人切片”并要求按其中固定范围执行。[TD-SLICE-CU-RH-01 rev1](spec/v0.1/TECHNICAL-DESIGN.md)采用独立稳定主体记录＋客户—主体关联，以有权客户为访问锚点，复用现有`customer.read`／`customer.edit-routine`完整Grant；关联已有主体还须先通过可读客户到达，响应不暴露其他客户关系。设计已覆盖多对多逻辑模型、组合部门约束、原客户详情页与嵌套只读详情、五个HTTP契约、事务／版本／幂等／审计／错误、三段实施任务及Q2验收；不新增主体自动查重。业务代码、Prisma schema、迁移、接口、页面和可执行主体测试在该设计轮均未创建；主体编辑、删除、解除关联、默认主体、共享主体编辑、多联系人、权利资产、协议、合作状态、生产变更和真实数据均排除。其“主体名称必填，信用代码／地址／法定代表人／职务可空”建议已由后续CUST-RH-001获得确认和编码授权。E01只阻断真实身份联调，E02不阻断该无文件切片。

GOV-RISK-GATES-001已完成：用户于2026-09-17明确授权开发流程去重与风险分级，范围仅为候选证据复用、数据库验证触发、独立审查和自动集成规则，不启动业务功能，不改业务代码、schema、迁移、依赖或测试服务。唯一完整规则为[.cursor自动集成规则](../.cursor/rules/verified-feature-integration.mdc)；`AGENTS.md`仅保留入口，本规范与README只说明调用关系。现有命令已能支持分级验证，因此未新增缓存、调度脚本或依赖；`pnpm verify`包含上下文、Spec、类型、Lint、格式、工具／前后端测试与构建，不包含数据库E2E。本任务按Q2执行旧门禁；基线为`0f66e34`，已验证候选为`65da7de`，tree为`942679e`，精确候选`pnpm verify`退出0、用时61.9秒。独立Q2首轮Critical 0、Important 5、Minor 1；修正后最终结论为`ACCEPTED`，Critical／Important／Minor均为0。数据库E2E未运行，因为候选未触及业务运行时、数据库路径、迁移、权限、事务或被测服务。现在只剩已授权的安全集成与远端核实；本段不提前宣称已推送。

CUST-FND-008第三批首个纵向切片已获用户于2026-09-17明确授权并完成：在现有Customer聚合、权限范围、乐观版本、共享审计和运营端页面上维护一位准入联系人，字段仅为姓名及电话／邮箱至少一种。草稿可完全不填；填写时执行服务端空值和格式校验。联系人随客户TEAM／SELF／部门范围，不创建账号或权限，修改与客户版本及脱敏审计同事务。实施计划见[客户准入联系人计划](superpowers/plans/2026-09-17-customer-admission-contact.md)。本切片明确排除多联系人、主要联系人、删除策略、正式准入、材料／E02、真实登录／E01、权限平台、其他业务模块、生产迁移和发布；本轮不搭建`dev:acceptance`或执行人工localhost可视化验收。首个固定候选`5b88467`的独立Q2发现1项Important和1项Minor；修复提交`a3ee91e`增加`BTRIM`／`NULLIF`数据库约束、原始SQL负向回归及姓名／电话审计脱敏断言。独立复查结论为`ACCEPTED`（Critical 0、Important 0、Minor 0），空库7份迁移、完整`pnpm verify`和数据库型Playwright 27/27均通过；当前状态为`IMPLEMENTED_VERIFIED_INTERNAL_INTEGRATION`，不代表已上线。

GOV-AUTO-VCS-001的已批准自动版本管理并入GOV-RISK-GATES-001唯一规则入口：只有必要门禁通过、独立审查`ACCEPTED`且Critical／Important／Minor均为0才能自动集成并推送。冲突、脏工作区、异常分叉、鉴权或门禁失败时保留现场；不包含强推、标签、Release、部署、生产迁移或生产数据。

CUST-FND-002～004首批实现已取得内部数据库与跨端证据。独立Q2前两轮结论为`BLOCKED`，对应Important项已通过前向迁移、服务端约束和真实数据库负向测试修复；2026-09-17最终独立复查结论为`ACCEPTED`，Critical 0、Important 0。该结论接受当前内部实现范围，不包含E01真实身份、E02对象存储、真实旧库恢复／生产迁移或上线许可；首批状态为`IMPLEMENTED_VERIFIED_INTERNAL_INTEGRATION`，不能简称为生产`VERIFIED`。

CUST-FND-005～007第二批按[最小实施计划](superpowers/plans/2026-09-17-customer-foundation-second-batch.md)实施，范围仅为客户草稿基础信息编辑、既定判重和修改审计。当前已有`customer.edit-routine`动作、部分PATCH＋乐观版本、NFKC／空白规范化判重、同部门精确证件唯一约束、同名原因、重复摘要和跳转、同事务安全前后值审计、运营端编辑页和原详情页入口；六份迁移在重建的独立PostgreSQL测试库从零实跑且schema最新，数据库型Playwright 25项通过。正式准入、联系人、材料版本、权限管理页面和其他业务模块未进入本批，也未发布上线。

GOV-LIVE-SPEC-001已完成：用户于2026-09-16引用会话“收口字段流程 Design”，明确要求对已批准范围直接修正、验证并交付，不再重复请求同一确认。本轮已修正客户材料精确版本、就绪门禁、陈旧权限／版本摘要、历史关系留存和重复审计事实；把最短有效执行链写入现有AI入口；以测试驱动实现`pnpm spec:check`；安全清理已批准的7个可再生目录；并拆出[首批客户基础小闭环任务](superpowers/plans/2026-09-16-customer-foundation-first-batch.md)。首次`pnpm verify`发现嵌套命令回落到Codex运行时，增加项目pnpm shim并将`.cmd`纳入上下文跟踪后完整复验通过。正式业务schema、API、迁移、页面、真实接入和生产操作仍未授权。

TD-TRACE-UX-001已完成设计：改动前完整工作区已保存为本地基线提交`a7649ff`（`docs: save approved system design baseline`）。用户于2026-09-16引用会话“收口字段流程整理”批准在不删除、不大改现有业务流程且不改变业务人员操作习惯的前提下，补强自动留痕、材料精确版本、统一权限校验、原页面就地提示、检索与防重复能力。TD-BASE-05／TD-TRACE-UX-01已把A01～C04映射到现有领域和模块，并加入材料V1/V2、三类时间、撤权、合并、并发退款、幂等和备份恢复验收。本轮只更新现有Spec、技术设计、追踪与验收，未修改代码、数据库、迁移或Demo；正常新建、上传、阶段办理和费用录入的新增必填字段、必经页面和强制确认均为0。

BR-007／TD-AUTHZ-001已完成：用户于2026-09-16批准引用会话“检查开发依赖”的五项建议，并明确要求案件和角色权限可随时灵活调配。SD-28～33已把固定角色矩阵改为可编辑初始模板，并确认操作＋数据范围Grant、案件成员、授予上限、下一请求生效与审计；同时收口客户主体证件／最低联系人、优先SSO／OIDC、私有对象存储限制及共享库部门强隔离。TD-BASE-04、TD-AUTHZ-01和客户基础rev2已形成可评审设计；本轮未实现账号、权限后台、schema、迁移、文件存储或业务页面。

TD-CU-001已完成：依据用户2026-09-16“根据回答执行”的确认，已在既有DOMAIN、DECISIONS、READINESS和TD-BASE-03上产出[客户基础TD-SLICE-CU-BASE-01](spec/v0.1/TECHNICAL-DESIGN.md)。范围仅含客户列表、新建／编辑、判重、草稿保存、资料准入校验、详情，以及必需的文件、权限、审计依赖；采用单一Customer Module和四个直接页面，不扩展案件、结算、审批中心、通用工作流或完整文件中心。设计可评审；客户动作权限／范围、正式证件口径、E01、E02及DD-09仍阻断完整编码，本轮未创建业务schema、迁移、接口、页面或测试。

BR-006已完成：用户于2026-09-16确认BR-04补问全部采用建议方案，并要求保持简单易用。SD-22～27已固化客户判重／无证件准入、正常阶段与历史补录、案件权限与旧律师可见、文件恢复、金额／退款及案件合并规则；字段、关系、流程、模块REQ/AC、技术基线、准入和后续设计清单已同步。受控动作由有权角色直接执行并填写原因，不新增审批单、审批队列或多级审批状态；合并只建立可解除的办理组，不搬迁或复制源案数据。客户基础、历史补录、合并办理组、金额基础和文件生命周期可分别进入TD-SLICE设计评审；本轮未实现业务代码、schema、迁移或页面。

BR-005已完成：业务返回的BR-03工作簿已按“明确接受／明确修改／部分回答／明确暂缓／仍待确认”逐子问题回填现有Spec；原件、SHA256和单元格来源已登记，BQ-01～11缺口、决定页历史等待措辞及TD-SLICE准入已同步。BQ-02整项接受；其余只关闭回件明确覆盖的子问题。审核人和审核日期为空，不代填。当前没有完整业务切片达到编码准入；本轮只完成字段、流程、依赖和可评审技术准备，不创建正式业务API、数据库表／迁移或业务代码，不修改Demo。

CTD-001已完成：已在[DOMAIN](spec/v0.1/DOMAIN.md)收口字段组→BQ→TD依赖，在[ACTIONS](spec/v0.1/preparation/ACTIONS.md)收口正常路径、分支、业务阻断与技术依赖，在[READINESS](spec/v0.1/READINESS.md)区分TD-BASE、TD-SLICE和编码准入；新增唯一[业务无关技术设计TD-BASE-01](spec/v0.1/TECHNICAL-DESIGN.md)，完成T01～08的共用接口、一致性、数据范围、金额日期、文件/OCR、投影及迁移边界。本轮未实现业务实体、API、数据库表／迁移、权限或工作流，未改Demo。

BR-004审核入口已经收到回件并由BR-005处理；其补问已在BR-006由用户整体采用建议。[BR-05业务回件](spec/v0.1/BUSINESS-REVIEW.md)保留11组原始回件状态、SD-22～27后续确认和仍按切片开放的事项。16个正常阶段门槛已整体采用；这不自动补齐二审／重审、执行结束、结案资金、证物实物、完整结算或报表口径。

BR-003的26题Excel与BR-02的36题Markdown稿保留为历史验证记录，不再作为当前业务审核入口；其中已填写结论已迁入SD-09/10，没有因精简表格丢失。工程设计、环境配置和外部依赖不交给业务人员判断。

DOC-001：整理生成文档。已将Spec模块目录并入[总入口](spec/v0.1/README.md)，删除重复进度页并精简本页历史流水；当时Spec由27份减为25份。49REQ、51个未执行目标AC、12项BD与7项SD决定不变；固定Demo五文件、41份反向证据与已有工程差异保留。当前仍是需求审核，未批准任何完整业务模块进入TD-SLICE或编码。

有效成果：[固定反向事实](demo-reverse/v158/AS-IS.md)、[目标Spec](spec/v0.1/README.md)、[主链准备包](spec/v0.1/preparation/README.md)。原反向两次人工检查已通过；后续不重复检查点。SPEC-001/002历史核查与交付证据见[验证记录](spec/v0.1/VALIDATION.md)和[一致性记录](spec/v0.1/CONSISTENCY.md)，不在状态入口重复全部经过。Demo整理删除的旧包等已按DEMO-001进入回收站，当前有效Demo为5文件。

DOC-001历史整理结果：当时25份Spec的本地链接有效、49REQ／51AC仍在、46项固定哈希一致；完整环境与验证记录见VALIDATION。其文件数和快照数是当时范围，不代表BR-005当前规模。

## 已实现

| 能力       | 当前事实                                                              | 边界                         |
| ---------- | --------------------------------------------------------------------- | ---------------------------- |
| 工程       | Vue/Nest/pnpm模块化单体；权限与客户草稿三份迁移已在测试库执行         | 共享／生产迁移未授权         |
| 接口与日志 | 健康/OpenAPI、统一错误；客户草稿／范围读取与共享审计已实现            | 真实身份E01尚未接入          |
| 浏览器 API | JSON GET/POST/PUT/PATCH/DELETE、204、超时取消、健康 API 运行时校验    | 不含上传、会话或业务字段错误 |
| 页面       | 工程健康状态；运营端客户列表、新建草稿、详情／折叠历史；数据库E2E通过 | 真实登录E01尚未完成          |
| 组件兼容   | 版本绑定的声明补丁、全量入口类型回归和独立交互夹具                    | 新控件仍需对应业务交互测试   |
| 版本与检查 | 本地 Git、文档/补丁快照、类型/Lint/格式/单元/契约/构建/E2E            | 无远程强制检查               |

## 未决与限制

具体事项仅在 [后续设计清单](deferred-design.md)维护，此处按输入分组。

- **仍按切片确认的业务项**：线下调查／直诉／混包、费用类别与收付方、二审／重审／执行结束、结案资金、证物／公证书实物、结算公式与完整状态、报表口径；只阻断各自范围。详见[业务回件](spec/v0.1/BUSINESS-REVIEW.md)、[缺口](spec/v0.1/GAPS.md)和[就绪清单](spec/v0.1/READINESS.md)。
- **E01真实身份单独阻断**：需要实际身份提供方及OIDC／SSO协议参数、issuer/client/audience与claim映射、秘密交付路径、会话／退出／撤销规则、MFA与账号生命周期、测试租户和测试账号。只阻断真实登录与身份联调；当前合成身份仅用于`NODE_ENV=test`，不阻断本批内部客户规则验证。
- **E02对象存储单独阻断**：需要私有对象存储provider/endpoint/region/bucket、凭据交付路径、加密与密钥归属、签名链接策略、安全扫描边界、保留策略及备份恢复证据和测试bucket。只阻断证件材料、版本和正式准入；本批未写对象存储代码。
- **其他外部条件**：AI供应商、业财协议，以及OPS-01生产环境和CI-01托管平台。原则已定不等于可实施条件齐备。
- **CMP-01 已关闭，保留维护要求**：升级 Element Plus 时必须复验或移除版本绑定补丁，不能手改 node_modules 或放宽类型。见 [组件兼容报告](component-compatibility.md)。
- 工程仍仅适用本地；ESLint 9 和部分间接依赖的弃用提示保留，版本升级单独评估。

## 最近验证

本轮CUST-RH-001C：首先记录缺少主体表`P2021 / 42P01`的真实RED，迁移与fixture完成后主体专项10/10；随后固定候选`pnpm verify`通过工具19、后端98、前端95项及上下文／Spec／类型／Lint／格式／构建，完整数据库Playwright 37/37通过。空库8份迁移、上一schema含联系人旧客户2/2保留、两方向部门FK及中途失败无半成品均通过。最后只回填非执行性结果并重录上下文；独立Q2尚未执行，不集成、不推送、不上线。

本轮CUST-RH-Q2-FIX-001：独立Q2首轮对`70c4b41`给出REJECTED（Critical 0／Important 2／Minor 1），分别为数据库CHECK未覆盖tab／换行／Unicode空白、同一RouterView参数切换保留旧主体，以及投影E2E读写竞态。新增测试先复现后修复：前向080000迁移显式覆盖ECMAScript trim空白集合，直接SQL验证全部25个码点及组合均由命名约束拒绝，并验证存量脏值使迁移23514失败且事务完整回滚；真实memory RouterView覆盖客户／主体参数变化、404及乱序旧响应；投影断言改为关联前读取。隔离测试库从空服务顺序应用9份迁移并报告最新；修复后`pnpm verify`通过工具19、后端98、前端100项及全部静态／构建门禁，完整数据库Playwright 38/38通过。仅回填非执行性证据后待同一独立Q2主体复审；未集成、推送或上线。

本轮CUST-RH-DESIGN-001：项目Node v24.21.0、pnpm 11.27.0与锁定值一致，开工上下文检查通过。首次`pnpm spec:check`因技术设计标题未显式包含SD-34而失败，补回有效约束标记后通过53个REQ、55个AC、11个BQ和34个SD；范围自审与`git diff --check`通过。最终格式、上下文快照、本地链接及Git差异检查按收口命令记录在[验证记录](spec/v0.1/VALIDATION.md)。本轮只改设计／治理文档及上下文快照，未运行代码测试、构建、数据库E2E或迁移专项；独立Q2保留到获得编码授权后的实现候选门禁。

本轮GOV-RISK-GATES-001：实测Node v24.21.0、pnpm 11.27.0与锁定值一致。新隔离工作树初次缺少`node_modules`，用`pnpm install --offline --frozen-lockfile`复用本地缓存初始化一次，用时117.0秒，未下载或升级依赖。基线`pnpm verify`通过，用时69.3秒；最终候选`65da7de`的`pnpm verify`也通过，用时61.9秒，覆盖上下文199个文本、Spec 53REQ／55AC／11BQ／34SD、工具19项、后端68项、前端53项、类型／Lint／格式和前后端构建。完整verify共执行2次（基线1、候选1），没有因阶段切换重复；数据库E2E执行0次，原因是本任务只改验证／集成规则与说明，未改业务代码、schema、迁移、数据库逻辑或服务。独立Q2最终核对SHA、tree、快照和完整验证证据后结论为`ACCEPTED`（Critical 0、Important 0、Minor 0）。

本轮GOV-AUTO-VCS-001首次执行：已验证功能分支`codex/customer-admission-contact`以合并提交`db7e5d4`纳入`main`；合并前本地与`origin/main`差异为0／0且主分支干净。合并结果重新执行`pnpm verify`，上下文、Spec、类型、ESLint、Prettier、工具19项、后端68项、前端53项及构建全部通过；数据库型Playwright 27/27再次通过。GitHub已接受`7efdf56..5cb8464`并更新`origin/main`；该自动版本管理不包含标签、Release、部署、生产迁移或生产数据操作。

本轮CUST-FND-008：新增后端测试先5项RED、前端/API测试先8项RED；实现Customer聚合内一位准入联系人后，后端聚焦31/31、前端53/53和全仓类型检查通过。独立Q2对`5b88467`发现数据库CHECK只判断NULL、可被纯空白值绕过（Critical 0、Important 1）；修复将非空判断收紧为`NULLIF(BTRIM(...), '')`，并新增直接数据库负向测试和姓名／电话审计脱敏断言。只重建临时Compose服务`dev-cor-postgres-test-1`，Prisma从空库顺序应用7份迁移并报告schema最新；数据库型Playwright现为27/27通过，覆盖真实页面与PostgreSQL持久化、仅电话／仅邮箱、数据库空白约束、TEAM／SELF／部门范围、跨部门404、撤权403、旧版本409、客户／审计事务回滚和联系人敏感值脱敏。修复后的`pnpm verify`完整通过上下文、Spec、类型、ESLint、Prettier、工具19项、后端68项、前端53项及前后端构建；独立Q2复查`a3ee91e`为`ACCEPTED`（Critical 0、Important 0、Minor 0）。未搭建`dev:acceptance`，未做人工localhost验收，未接E01/E02，未执行生产迁移或发布。

本轮GIT-REMOTE-001：将已验证功能分支快进合并到本地`main`后，系统级`core.autocrlf=true`把未声明属性的文本检出为CRLF，导致Prettier行尾门禁稳定失败；新增仓库级`.gitattributes`将文本统一为LF，并保留既有补丁文件空行规则。格式化后索引与工作区均为LF；合并后的`pnpm verify`通过上下文、Spec、类型、ESLint、Prettier、工具19项、后端60项、前端49项及构建。该修复只稳定Windows／GitHub跨平台检出，不改变业务逻辑；远程目标为`https://github.com/glunek123/ip-bp.git`，推送不代表发布上线。

本轮CUST-FND-Q2-REMEDIATION／CUST-FND-005～007：新增测试先分别因缺少编辑服务／路由／页面及PATCH合并缺陷而RED；最终`pnpm verify`通过上下文、Spec、类型、ESLint、Prettier、工具19项、后端60项、前端49项及前后端构建。`postgres-test`健康，六份迁移从零应用成功且schema最新；最终数据库型Playwright 25/25通过，覆盖真实页面新建→编辑→详情、TEAM／SELF同部门隔离、跨部门不泄漏、成员组合外键、空团队防重、撤权、部分PATCH保留、`null` 400、NFKC／空白判重、隐藏同名不可枚举、当前客户排除、并发证件唯一性／同名串行化、版本冲突两种用户选择、审计前后值／短值完全掩码、迁移失败原子回滚及创建／修改事务回滚。独立Q2最终复查为`ACCEPTED`（Critical 0、Important 0）；测试身份不等于E01，测试库不等于生产迁移，不发布上线。

本轮CUST-FND-DB-E2E：Docker 29.5.3 Linux引擎恢复，`postgres-test`在127.0.0.1:55433健康运行；Prisma依次应用三份迁移并报告schema最新。新增数据库测试先证明测试身份未接入时全部401，并证明跨部门角色模板分配可被错误写入；受控测试身份Adapter与组合外键迁移完成后，Playwright 9项通过，覆盖真实客户草稿／审计历史、跨部门列表和不泄漏404、角色模板部门完整性、下一请求撤权、客户写失败不留审计、审计写失败回滚客户、健康页及组件交互。完整`pnpm verify`通过：工具19项、后端45项、前端39项、类型／Lint／格式／构建全部成功。实现者Q2自审已更新；独立复核、E01和E02未完成，不宣称可上线。

本轮CUST-FND-003：项目入口稳定加载Node v24.21.0／pnpm 11.27.0；新增测试先因客户服务、控制器和身份端口不存在而RED。实现后客户服务／HTTP聚焦测试10项通过，覆盖名称清理、服务端归属、同事务审计失败传播、范围谓词、稳定排序、未认证401、缺动作403、伪造部门字段400和不泄漏404。完整`pnpm verify`通过：上下文与Spec检查、工具19项、后端40项、前端24项、全仓类型／ESLint／Prettier及前后端构建均成功。`pnpm db:test:up`因本机Docker daemon不存在失败，未执行测试迁移、PostgreSQL跨部门隔离或真实事务回滚；状态保持`IMPLEMENTED_UNVERIFIED_DB`。

本轮CUST-FND-004：前端客户API／页面测试先因文件不存在RED；实现列表、新建、详情路由后，前端39项测试通过，覆盖响应解码、加载／空／失败、服务端创建能力、名称校验、防重复提交、失败保留输入、草稿详情和折叠历史。后端同时补齐详情共享审计投影、稳定403错误码和服务端创建能力。最终`pnpm verify`通过：上下文／Spec、工具19项、后端42项、前端39项、类型、Lint、格式和构建均成功。浏览器以拦截的合成响应走通列表→新建→保存→详情，桌面视口三页视觉检查无控制台错误；该检查只证明前端布局和交互，不替代真实身份、数据库、跨部门隔离或E2E证据。

本轮GOV-LIVE-SPEC-001：`pnpm spec:check`在5个故障样例上验证重复REQ、无效REQ／SD引用、表格列漂移和当前版本过期会失败；当前Spec实测53REQ、55AC、11BQ、34SD并通过。修复嵌套pnpm运行时后，`pnpm verify`完整通过：19项工具测试、后端23项测试、前端24项测试、类型检查、Lint、格式和前后端构建均通过；未运行E2E、数据库迁移或外部联调。已删除7个批准目录中的6918个既有文件／44,428,178字节，验证生成的43个dist文件／209,653字节随后再次删除；这些忽略目录不可从Git恢复，但可由构建／测试重建，环境文件、依赖、生成Client、Demo和锁文件仍在。完整证据见[验证记录](spec/v0.1/VALIDATION.md)。

本轮TD-TRACE-UX-001：27份Spec Markdown中53个REQ唯一且标题无重复，55个唯一AC引用、11个BQ和SD-01～34连续存在；本地链接失效和格式差异均为0。项目Node v24.21.0、Corepack pnpm 11.27.0与锁定值一致；最终上下文记录／检查、全仓Prettier及Git差异检查通过。仅修改规范、设计、治理文档及上下文快照；未改Demo或正式代码，未运行代码测试、构建、E2E、数据库或外部联调；详见[验证记录](spec/v0.1/VALIDATION.md)。

本轮BR-007／TD-AUTHZ-001：27份Spec Markdown中52个REQ唯一且标题无重复，54个AC引用、11个BQ和SD-01～33连续存在；本地链接失效和Markdown表结构错误均为0。项目Node v24.21.0、Corepack pnpm 11.27.0与锁定值一致；最终上下文记录／检查、受影响文档Prettier及Git差异检查通过，暂存区为空。仅修改规范、设计、治理文档及上下文快照；未改Demo或正式代码，未运行代码测试、构建、E2E、数据库或外部联调；详见[验证记录](spec/v0.1/VALIDATION.md)。

本轮TD-CU-001：已核对现有Vue／Nest／Prisma工程骨架和客户Spec；27份Spec Markdown中50个REQ保持唯一且标题无重复，52个AC引用、11个BQ和SD-01～27存在。项目Node v24.21.0、Corepack pnpm 11.27.0；最终上下文记录／检查、全仓Prettier及Git差异检查通过，暂存区为空。仅修改设计和治理文档及上下文快照；未改Demo或正式代码，未运行代码测试、构建、E2E、数据库或外部联调；详见[验证记录](spec/v0.1/VALIDATION.md)。

本轮BR-006：27份Spec Markdown中50个REQ保持唯一，52个AC引用、11个BQ和SD-01～27连续存在；本地链接失效和Markdown表结构错误均为0。最终上下文快照、全仓Prettier及Git差异检查通过，暂存区为空。仅修改Spec、后续设计清单、项目状态和上下文快照；未改Demo或正式代码，未运行代码测试、构建、E2E、数据库或外部联调；详见[验证记录](spec/v0.1/VALIDATION.md)。

本轮BR-005：已核对原始回件两张工作表、11组业务回复、16个正常案件阶段建议及空白审核人／日期；工作簿SHA256与登记值一致。静态审计为27份Spec Markdown、50个唯一REQ、52个唯一目标AC、11个唯一BQ、21项SD，本地链接和Markdown表结构错误均为0；最终上下文快照、全仓Prettier及Git差异检查通过，暂存区为空。仅改Spec、后续设计清单、项目状态、回件证据和上下文快照；未改Demo或正式代码，未运行代码测试、构建、E2E、迁移或外部联调；详见[验证记录](spec/v0.1/VALIDATION.md)。

本轮CTD-001：27份Spec Markdown、49REQ、51个目标AC和11个唯一BQ结构保持一致；153个跟踪文本的最终上下文快照与本地链接检查通过，全仓Prettier及Git差异空白检查通过，暂存区为空。仅改文档和快照，未改Demo或正式代码，未运行代码测试、构建、E2E、迁移或外部联调；详见[验证记录](spec/v0.1/VALIDATION.md)。

本轮BR-004：26份Spec、49REQ、51个目标AC、11个唯一BQ、16个案件阶段及46项固定证据哈希检查无错误，本地链接有效。新版Excel主表11项、辅助表16阶段；结论下拉、0/11完成统计、1/10临时试算和公式错误0项均通过，两页渲染人工复核通过。项目Node下格式、最终上下文快照和Git差异检查通过。仅改Spec、项目状态和业务审核工作簿；未改Demo或正式代码，未运行代码测试、构建、E2E、迁移或部署。

本轮SPEC-001：[文档验证](spec/v0.1/VALIDATION.md)记录49REQ/51AC、73候选/12BD/27FIX/12G/18U映射、23表列数和476本地链接结构检查无错误；46份固定文件哈希相同。独立源码核查、主链复核与全量审查的确定问题均修订。运行时24.21.0／11.27.0；150文本上下文检查、格式和Git差异检查通过，暂存区为空。仅改文档，未新运行浏览器、业务测试、构建、E2E或外部联调；未提交推送。文档交付不代表整个业务Spec已批准或可直接编码。

本轮DEMO-002：M01/M02原20个观察见[模块验证](demo-reverse/v158/MODULE-VALIDATION.md)；其余模块新增31个隔离观察见[后续验证](demo-reverse/v158/CONTINUATION-VALIDATION.md)，其中ET02有1个pageerror、DT02有捕获异常，均记录Demo问题，不称51项业务验收通过。[独立审查](demo-reverse/v158/REVIEW.md)7项文档错误修订后已复核。五文件哈希一致；41份提取文档、84张表的列数／表头及1034个行号引用的数值范围检查无错误（非逐条语义证明），格式、相对链接、126文本文件上下文快照、Git差异检查通过，暂存区为空。仅改文档，未重跑正式代码测试、构建或E2E。

本轮 DEMO-001：v158 三件套与供应包记录的 SHA256 一致，且与包内实际工作目录逐文件一致；整理后 `demo` 仅含 5 个正式文件。已执行 JavaScript 语法、入口资源引用、文档格式、相对链接、上下文快照和 Git 差异检查。供应包自带验证记录为冒烟 1578 通过 / 0 失败、静态表格 0 处不一致、视图内死按钮 0、严格孤儿 0；本次未保留也未重新运行其专用验证脚本，不把供应记录表述为本轮实测。未修改正式系统代码、配置或依赖，未重跑项目代码测试、构建或 E2E。

本轮 REQ-001 为文档登记任务：项目 Node 24.21.0、pnpm 11.27.0 与锁定版本一致；用户随后明确授权删除项目内全部 `.workbuddy` 内容，已删除其中的工作日志和空目录。文档格式、相对链接、上下文快照和 Git 差异检查通过。代码、配置、依赖和测试未改，未重跑代码测试、构建或 E2E。

本轮 GOV-001：context:check、format:check、Git 差异空白检查及规则一致性人工核对通过；回填结果后重新记录并检查快照，文档一起保存至本地提交。代码、配置、依赖和测试未改，按 AI 规范的文档任务流程复用 b3a8600 的代码验证证据，本轮未重跑代码测试、构建或 E2E。

CMP-01 历史证据：项目 Node 24.21.0 / pnpm 11.27.0 下冻结安装、pnpm verify（60 项测试及严格类型/Lint/格式/构建）、3 项 Chromium 测试通过，详见 [组件兼容报告](component-compatibility.md)。CMP-01 保持已关闭状态；新增治理规则不改变这些历史结果的适用范围。

## 下一步

| 顺序 | 任务                     | 依赖                                                                                       | 状态             |
| ---- | ------------------------ | ------------------------------------------------------------------------------------------ | ---------------- |
| 1    | 权利主体独立Q2与候选集成 | TD-SLICE-CU-RH-01 rev1；完整verify／E2E与迁移证据已齐；独立接受后按规则集成                | 待独立Q2／未上线 |
| 2    | 接入真实公司登录E01      | 身份提供方、OIDC／SSO参数、claim映射、秘密交付、会话／撤销／MFA、测试租户和账号            | 外部阻塞         |
| 3    | 接入私有对象存储E02      | provider／endpoint／region／bucket、凭据、加密密钥、签名链接、安全扫描、保留及备份恢复证据 | 外部阻塞         |
| 4    | 客户证件材料与正式准入   | E02；现有精确材料版本和准入规则；另行切片设计／编码授权                                    | 待设计           |
| 5    | 线索最小闭环             | 可供主链选择的客户—主体组合Query及线索范围确认                                             | 待设计           |

以上队列不把后续任务整体授权为可编码。真实旧库恢复、生产迁移和发布继续另行准入。
