# 当前开发状态

更新日期：2026-09-15。本文件是恢复开发上下文的第一入口；准确版本、命令及详细证据通过下文链接读取。

## 当前阶段

**通用工程底座已建立；Demo 部门审核中，尚未进入正式业务开发。**

用户最新边界：先反向整理 Demo 并确认需求，再开发正式业务。当前仅检查骨架、修正文档记忆与同步规范，不新增业务功能。

## 当前任务

任务：CTX-001，骨架范围复核与文档同步机制。状态：已完成。

- 已检查正式前后端入口、健康检查、schema、请求封装和运行配置；未发现业务模型或权限角色混入。
- 已建立本状态入口、[AI 开发规范](ai-coding.md)、文件漂移检查与根 verify 命令；独立骨架审查已结束，见 [复核报告](foundation-review.md)。
- 整体 verify 通过：文档同步 12、前端 9、后端 17 项测试通过，类型、Lint、格式和构建通过；另有 2 项 Chromium 跨端测试通过。
- 独立同步机制复核完成；已明确链接检查仅支持简单行内文件链接。本任务无未完成实现；业务开发继续等待已确认需求。
- 中断恢复：先读取本文件及 AGENTS.md，加载运行入口，执行 `pnpm context:check`。快照未建立或不一致表示需要核对，不表示可以直接重置。

## 已实现

| 能力       | 状态与源码入口                                                   | 适用边界                                                    |
| ---------- | ---------------------------------------------------------------- | ----------------------------------------------------------- |
| 前后端工程 | 根 workspace；frontend/src/main.ts；backend/src/main.ts          | Vue/Nest 模块化单体的本地工程骨架                           |
| 数据库基础 | backend/src/database；backend/prisma/schema.prisma；compose.yaml | Prisma CJS 与独立 PostgreSQL 开发/测试库；无业务表          |
| 接口基础   | backend/src/common；backend/src/health                           | 请求编号、输入校验、统一错误、健康检查/OpenAPI              |
| 浏览器请求 | frontend/src/api/http.ts                                         | 仅 GET JSON、超时、取消和错误处理；尚无写请求或附件上传封装 |
| 工程页面   | frontend/src/app/HealthPage.vue；styles                          | 健康状态、错误和重试；名称与配色可替换，不是业务工作台      |
| 验证工具   | package.json；测试配置及 tests/e2e                               | 已建立类型、Lint、格式、单元/契约、构建和跨端检查           |

第三步原始验收见 [工程验收报告](engineering-verification.md)，该报告对应其记录的历史候选版本，不随之后的代码变化自动成为新验收。

## 未决与限制

- 业务实体、字段、流程、角色与数据权限、金额口径、附件、AI 和业财协议待需求确认。没有业务 schema 或 migration。
- HTTP 写请求、字段级校验反馈、鉴权注入等在相应需求出现时补充；不能把当前 GET 骨架当作完整业务接口层。
- 日志尚缺安全的业务事件/路由与代码位置；健康结果校验暂在页面。首个业务模块落地时分别完善诊断与 api 层职责，详见 [复核报告](foundation-review.md)。
- 当前配置仅用于本机；生产监听、跨域/反向代理、密钥注入、备份恢复、监控及部署流程尚未设计或验证。
- Element Plus 全量类型入口存在已记录兼容问题，当前实际使用的按钮按需导入并通过检查。新增控件需实测。
- 工具链沿用已选版本；ESLint 9 与部分间接依赖有上游弃用提示，升级需单独评估。
- 尚无 Git 仓库、远程或托管 CI。当前校验为本地门槛，没有版本回退或服务器强制保护。

## 最近验证

本轮 2026-09-15 实际执行 `pnpm verify` 成功（文档一致性、类型、Lint、格式、38 项工具/前后端测试、构建）；`pnpm test:e2e` 使用独立 PostgreSQL 测试库，2 项 Chromium 测试通过，覆盖真实数据库健康链路及故障重试。运行时为项目 Node 24.21.0 / pnpm 11.27.0。

验证后只更新文档，按交付流程重新记录快照并检查一致性和格式。当前文件指纹见 context-snapshot.json；该文件仅证明检查点一致，不代替以上测试证据。详细范围与限制见 [本轮复核报告](foundation-review.md)，第三步历史证据仍保留在 [工程验收报告](engineering-verification.md)。

## 下一步

等待用户指定 Demo 反向整理材料与范围，按 [AI 开发规范](ai-coding.md)提取待确认需求。获得对应业务确认后才新增模块、接口和迁移；复用现有工程，并在首个相关模块中补齐上列对应能力。
