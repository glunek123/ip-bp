# 当前开发状态

更新日期：2026-09-20。本文件只保留恢复当前工作的最小事实；截至2026-09-18的流水见[历史状态](project-status-history-through-2026-09-18.md)，各候选的详细历史证据见[验证记录](spec/v0.1/VALIDATION.md)。两者均按需读取，不是新任务默认上下文。

## 当前阶段

首批正式业务开发：认证与客户最小主数据链路已形成内部闭环；Next Slice前置的Team主数据与管理授权边界已完成。业务能力范围和唯一Next Slice以[功能开发路线图](feature-roadmap.md)为准；生产交付能力与业务Slice分开管理。

## 当前任务

**GATE-HARDENING-V2（隔离分支`codex/gate-hardening-v2`）**：加固正式Slice证据的环境快照、最新尝试状态、候选边界与共享E2E互斥，修复冷worktree准备顺序，并治理流程文档重复。本任务不改变业务权限、接口、schema、依赖版本或生产环境；最终门禁与Review结果使用绑定候选的树外记录，不为回填结果再次改变候选tree。

三个集成位置必须分开判断：

- 流程治理改造已位于远端`origin/main@9bb2d9f`。
- Team实现位于本地`main@e6caef4`，2026-09-20重新fetch后远端`origin/main`尚未包含该提交。
- 本轮加固只位于上述隔离分支，尚未合并、推送或发布。

## 已实现

- 系统统一名称为“品维·知产业务管理系统”，同一系统服务品维部与知产部，服务端承担部门数据隔离与授权。
- 本地账号登录／会话、客户草稿建档／维护、一位准入联系人和权利主体最小关联已形成明确范围内的内部闭环。
- Team作为部门直属主数据约束成员、角色分配和客户的部门一致性；管理Action与Grant Boundary不以首位管理员、角色名称、固定ID或前端菜单绕过。
- 工程已有锁定运行时、上下文与Spec检查、类型／Lint／格式／单元／契约／构建门禁，以及使用独立测试库的Playwright入口。

## 未决与限制

- 下一业务Slice“人员账号、部门成员与角色分配闭环”仍属于Level 3；当前治理任务不实现其UI/API或角色模板Grant编辑。
- E01后续人员管理／MFA／找回密码／OIDC、E02私有对象存储、生产环境与CI仍按[后续设计清单](deferred-design.md)和路线图推进。
- 项目没有可靠依赖图，不实现自动affected验证；新的组合tree按三级规则执行所属层级门禁。
- 正式证据的命令边界检查和停止编辑的隔离worktree不能抵御所有具有本地写权限的进程；跨独立克隆的共享E2E锁只覆盖本机固定数据库与端口。
- 生产备份、正式迁移、部署、真实回滚和生产数据操作不在当前授权范围。

## 最近验证

- Team候选`e6caef4`的完整`verify`、独立测试库迁移／Playwright及Level 3复审曾通过；精确检查数量、tree与审查过程保留在[验证记录](spec/v0.1/VALIDATION.md)，不在状态页重复维护。
- 本轮加固的正式候选、完整门禁、数据库E2E和Review结果尚未写入仓库；以最终树外交付记录为准，不得预先视为通过。

## 下一步

固定并验证本轮加固候选，完成Level 3 Review；是否集成／推送由维护者另行决定。治理任务结束后，业务侧Next Slice仍为“人员账号、部门成员与角色分配闭环”。
