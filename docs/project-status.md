# 当前开发状态

更新日期：2026-09-15。本文件是恢复上下文的第一入口；历史报告只对当时版本有效。

## 当前阶段

**通用工程底座维护；Demo 部门审核中，尚未进入正式业务开发。** 没有业务实体、表、迁移、权限或工作流实现。

## 当前任务

任务：GOV-001，长期 AI 开发治理规则完善。状态：已完成。

- 依据用户提供的“确认Demo完善计划”评审补充五类规则与 CodeGraph 回退；基线 b3a8600，开工快照一致且 Git 工作区干净。
- AGENTS 改为有确认依据的阶段规则，补充工作区/危险操作边界和安全、发布的必读入口。
- AI 规范明确验证后改代码须重验、仅文档改动如何复用证据并完成最终快照/暂存检查。
- 开发规范补充真实数据、后端授权、迁移恢复及上线准入；这些是执行要求，不代表生产部署或业务能力已实现。
- 已核对阶段授权、工作区保护、验证闭环、安全、上线准入和 CodeGraph 回退六项覆盖，与既有规范一致；文档链接/格式/快照及差异检查通过。本轮只改 Markdown 文档和配套快照，不新增业务实现或检查脚本。

## 已实现

| 能力       | 当前事实                                                           | 边界                         |
| ---------- | ------------------------------------------------------------------ | ---------------------------- |
| 工程       | Vue/Nest/pnpm 模块化单体与本地 PostgreSQL                          | 无业务表和迁移               |
| 接口与日志 | 健康/OpenAPI、输入校验、统一错误、请求编号、路由模板和安全位置     | 无业务审计或生产监控         |
| 浏览器 API | JSON GET/POST/PUT/PATCH/DELETE、204、超时取消、健康 API 运行时校验 | 不含上传、会话或业务字段错误 |
| 页面       | 工程健康状态、加载、错误、重试和取消                               | 无业务工作台                 |
| 组件兼容   | 版本绑定的声明补丁、全量入口类型回归和独立交互夹具                 | 新控件仍需对应业务交互测试   |
| 版本与检查 | 本地 Git、文档/补丁快照、类型/Lint/格式/单元/契约/构建/E2E         | 无远程强制检查               |

## 未决与限制

具体事项仅在 [后续设计清单](deferred-design.md)维护，此处按输入分组。

- **等待 Demo 字段/规则确认**：DD-01 至 DD-08 的实体、流程、权限范围、页面/API、金额/报表、附件、AI/业财业务映射。
- **另待外部条件**：登录方式、存储、AI 供应商、业财协议，以及 OPS-01 生产环境和 CI-01 托管平台。Demo 文档到齐不等于这些条件齐备。
- **CMP-01 已关闭，保留维护要求**：升级 Element Plus 时必须复验或移除版本绑定补丁，不能手改 node_modules 或放宽类型。见 [组件兼容报告](component-compatibility.md)。
- 工程仍仅适用本地；ESLint 9 和部分间接依赖的弃用提示保留，版本升级单独评估。

## 最近验证

本轮 GOV-001：context:check、format:check、Git 差异空白检查及规则一致性人工核对通过；回填结果后重新记录并检查快照，文档一起保存至本地提交。代码、配置、依赖和测试未改，按 AI 规范的文档任务流程复用 b3a8600 的代码验证证据，本轮未重跑代码测试、构建或 E2E。

CMP-01 历史证据：项目 Node 24.21.0 / pnpm 11.27.0 下冻结安装、pnpm verify（60 项测试及严格类型/Lint/格式/构建）、3 项 Chromium 测试通过，详见 [组件兼容报告](component-compatibility.md)。CMP-01 保持已关闭状态；新增治理规则不改变这些历史结果的适用范围。

## 下一步

等待字段、规则文档；收到后登记版本、来源、确认依据，检查后续清单并建立需求—设计—实现—测试映射。未齐备项保持等待，不直接批量生成业务代码。CMP-01 不再阻塞后续选择器/表格页面开发。
